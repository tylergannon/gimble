{
  "name": "external-steering",
  "goal": "Prove that a live Codex turn receives and follows external steering",
  "defaults": {
    "model": { "name": "gpt-5.6-sol", "effort": "low" },
    "timeout": "90s"
  },
  "start": "work",
  "nodes": [
    {
      "id": "work",
      "type": "agent",
      "prompt": "Run one foreground shell command that writes ready followed by a newline to steering-ready.txt, sleeps for 15 seconds, and then exits. Wait for it. An external operator will send a steering instruction while that command is running. After it finishes, follow the new instruction exactly. Do not create steering-received.txt unless the external instruction requests it. Keep final notes to one sentence.",
      "fidelity": "none",
      "edges": [{ "to": "verify" }]
    },
    {
      "id": "verify",
      "type": "command",
      "command": "test \"$(cat steering-received.txt)\" = GIMBLE_STEERING_RECEIVED",
      "edges": { "success": "success" }
    }
  ]
}
