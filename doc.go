// Package gimble is a library for writing agent workflows as ordinary Go,
// with a web page that shows every run live.
//
// The API and the reasons behind it are in ephemeral/research/api/API.md;
// the build order is in ephemeral/research/api/SPRINTS.md.
package gimble
