# Issue 130 V2 UI proof worklog

correction: The pinned checkout's 15 protocol-v2 browser tests wrap legacy message.* payloads; they do not prove applyV2, projectV2, or native session.* rendering.

decision: Native UI evidence is anchored to OpenCode v2 commit c55ee2a8152603f04a409163bd3edf79c425fbd7 and must traverse packages/client/src/solid/data.ts into packages/session-ui.

friction: Frozen install left locked React packages in node_modules/.bun without root links, so the CLI failed on react/jsx-dev-runtime -> local node_modules links restore CLI startup without changing tracked upstream content.

decision: The staged SSE fixture proves schema-decoded native event reduction and rendering, but cannot support a backend-emission claim; the report states that boundary explicitly.

correction: Although no external provider credentials were available, upstream ships `script/drive/shell-output.ts` against the published `opencode-drive` controller; this enables deterministic model output through the real backend and must be checked before declaring native production blocked.

decision: The upstream Drive route produced and captured the full native session lifecycle from the real backend, including real shell execution; label the model as simulated and keep this SSE evidence separate from the browser reducer evidence because Drive exposes no browser attachment helper for its private ephemeral service.
