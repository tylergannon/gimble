correction: The website must not describe Gimble as an implementation of Attractor. It is a reaction to that work and must explain the current product plainly: what it is and how to use it.
doc_bug: Abstract language such as "stricter cost razor" hides the product rather than explaining it -> replace all website copy with concrete, current behavior and usage.
decision: Build a semantic index over docs/ and ephemeral/ before rewriting the website, with the index stored outside the indexed corpus at /private/tmp/gimble-docs-ephemeral-index.
friction: skgo rejects an unimported generated remote module and requires at least one Go declaration in a web app -> replace the greeting command with one read-only guide identity query, imported by the page.
