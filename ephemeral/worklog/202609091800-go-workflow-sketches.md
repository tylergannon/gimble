decision: Tyler proposes additive Go workflows while retaining the DAG language, workflow-specific Polytype argument schemas, and workflow-defined role names instead of model settings at call sites.
decision: Tyler prioritizes a small curated builtin catalog with discoverable and dependable invocation contracts for long-term scheduled calls; public library authorship is secondary.
finding: graph defines representations; reusable execution is in engine and harness, with loop/fanout/control/lifecycle still coupled to Runner. Extract operations incrementally rather than wrapping graph construction as the new language.
finding: Readable translations must preserve isolated bake-off worktrees, runtime-owned checklist validation/done, scoped advisory supervisors, and per-activation limits. Existing graph checkpoints do not imply arbitrary Go-stack resumption.
proposal: Keep contract versions distinct from implementation revisions so maintained methods can improve without silently changing old input semantics, outputs, or allowed effects. API sketches and the proposed CLI are hypothetical, not accepted or implemented.
correction: Translation review caught a local retry counter that would reset after failed runtime validation. Bind named implementation/review limits to the shared item scope so repeated helper calls preserve the existing per-item ceilings.
correction: Tyler identified wrong indirection and asked for the earlier POC. Main-only source inspection missed the preserved codex/programmatic-workflow-research branch (4906a98), initial POC 983ebc6, algorithm extraction ae7af1f, and concurrency sketches b04cd90. Recovered 15 source/design files from the history backup into a separate temporary inspection folder; no old history imported into current Gimble.
finding: The archived Research programmatic workflow DSLs task explicitly requested Loop as iter.Seq/iter.Seq2, input/prompt separation, automatic scoped context, and ordinary goroutines/errgroup for critique circles and bake-offs. Consult this implemented POC and its follow-up sketches before proposing replacement APIs. Current semantic-index entrypoint did not route to those preserved artifacts.
decision: At Tyler's request, extend the semantic index and direction documentation to route code design through the recovered POC, then earlier concurrency sketches, then the newer proposal with its correction. Preserve an exact 88-line source snapshot and original concurrency document in Markdown so navigation does not depend on temporary extraction paths.
finding: Independent index navigation reached the POC recovery/source, proposed errgroup concurrency, and newer roles/input/catalog notes with correct status distinctions. The existing evaluator's 13 queries report reachable routes, but its scoring is a mechanical smoke check, not proof of semantic retrieval quality.
decision: Tyler asked for actual compiling example programs with all operations stubbed and ordinary errgroup.Group parallelism. Keep these isolated in examples/go-workflows; derive small synchronous primitives and the iterator from the recovered POC, with no graph construction or generic fan-out facade.
correction: The source critique-circle YAML is independent drafts followed by peer critiques, not the earlier hypothetical revision/adjudication loop. The bake-off integrates its selected candidate and checks the integrated result. Preserve those sequences visibly in Go.
decision: Argument types belong to each example and contain no model settings. Use concrete local nested types so eventual Polytype schemas describe only the workflow input; the executable examples do not implement schema generation or a production catalog.
decision: Tyler asks to model SetContext as filesystem-backed values with inline-small/context-budget projection, spilling values behind semantic-index routes. Agentic calls should wait for coherent index updates; show the resulting prompts at successive stages. Keep broad open-source index research as follow-up rather than solving the production index now.
proposal: The executable example persists a value on SetContext and invalidates the current projection, then coalesces/rebalances pending updates synchronously at agent entry. This preserves read-after-write and permits unrelated ordinary Go work without introducing a background indexing scheduler. Bytes and deterministic size-based selection are explicit stand-ins for token-aware semantic rebalancing.
decision: Tyler requests automatic chapter/sprint context scopes plus arbitrary named scopes. Use the same Scope primitive underneath the chapter and sprint iterators so validation and workflow calls see the same item scope.
proposal: Scope entry copies the effective parent values and reuses their immutable files. Child writes and subsequent parent writes are isolated; scope exit means returning to the parent Go context, with no global push/pop or implicit result promotion. Item scopes persist across retries inside one loop activation; each later activation gets fresh scopes.
decision: Tyler treats physical context scopes as trusted information routing rather than a security boundary. Layered/branching filesystem structure should keep parallel write ownership simple; reading other layers is acceptable. The prototype nests scope directories, directs reads through the effective index, and writes overrides to the local layer while inherited value files remain unchanged by the API.
finding: Automatic all-item validation creates sibling contexts before selecting the first item. They therefore freeze the parent at their first validation, not at later first implementation; explicit parent updates are visible only to scopes created afterward. Document this distinction instead of implying dynamic inheritance.
correction: Review caught loop-owned Done metadata remaining stale after validation changed checklist state. Refresh the item's scoped value on reconciliation, and verify the yielded item and its on-disk scope agree even when a pre-marked Done value is rejected.
decision: Tyler asks whether a custom FUSE view over memory or opaque storage could vary by requestor, while suspecting a simpler option. Treat this as a design question; keep the working scope/index prototype and investigate explicit per-scope filesystem views before proposing a driver or identity-dependent mount.
finding: Primary FUSE sources document request-identity limitations and caches that can bypass userspace handlers; macFUSE FSKit lacks fuse_context_t. Recommend explicit per-scope revision paths. A flattened read-only directory is a possible next publication layer over the existing manifests; symlinks alone do not supply copy-on-write, and no materialized view or FUSE driver is implemented here.
decision: Tyler accepts ordinary scope views made from symlinks and copying changed values as the simpler alternative to FUSE. Extend the example to publish real view/values/<key>.json symlinks and view/index.json; retain immutable original value files. SetContext owns copy-on-write by creating a fresh value and subsequent view. Direct writes through symlinks do not copy and remain outside the managed context-write contract.
question: Tyler raises precedence when a parent changes after a child override. The current prototype freezes all inheritance at scope creation; later parent writes do not reach existing children. CONTEXT-SCOPES.md records that behavior separately from the agent's unaccepted live lexical inheritance proposal (local wins, latest parent otherwise, coherent snapshot per call).
proposal: Distinguish intentional overrides from values derived from stale parent data. Freshness needs explicit invalidation, rebase, or adjudication rules; symlink views and lexical precedence alone cannot infer them. No live inheritance or automatic freshness behavior has been implemented.
finding: Tyler supplied BranchFS; source assessment pinned a4b6592d31aacb4d2f94d0c80f43d47d38063fa9 in CONTEXT-SCOPES.md. It freezes inherited trees using file copies and explicitly commits leaf branches. Branch-wide commit counters detect sibling commits but not direct parent writes, so a child commit can replace a later direct parent edit. Sequential publication does not prove durable all-or-nothing behavior; macOS requires macFUSE, while integration CI is Ubuntu-only. No adoption or FUSE runtime test; workflow promotion and freshness rules remain ours to define.
correction: Final view review found that distinct ASCII keys such as goal and Goal collide on case-insensitive filesystems after both setters have succeeded. Reject case-insensitive collisions under the context lock before writing or advancing a revision; preserve readable filenames, same-key replacements, and the last valid snapshot. A regression verifies rejection does not poison later publication.
decision: Tyler simplifies context mutability to one owning scope per value; descendant writes to values written above must error. Implement this in the example backend, keeping ownership separate from frozen inheritance and preserving ordinary Go concurrency.
correction: Tyler rejects the proposed intraprocedural analyzer as insufficient and misleading; any adopted checker must traverse the call graph. The agent had already written it. Tyler explicitly asks to preserve written software while reining in scope, so retain the analyzer as unadopted reference code, not a workflow check, and defer call-graph work. Keep the primary deliverable compiling workflows with mocked agent/backend effects.
decision: Tyler accepts the correction to declared ownership and inherited values refreshed at each agent entry, asks for fixes, and requests consensus reviews via gimble run-prompt with Fable 5.1. Keep backends stubbed; challenge speculative requirements and complexity rather than following a design loop.
correction: Replace first-successful-writer ownership with an explicit scope-bound Key from DeclareContext. Existing item scopes read current ancestors at SnapshotContext; each agent receives its fixed view in Call.Context. This permits chapter updates to reach later sprints without altering prior prompts or turning scope labels into binding identity.
correction: Fable independently verified declared ownership and refreshed inheritance. Its filename finding exposed documentation ambiguity: any same-name collision qualifies all effective keys, so consumers follow the current index. Correct that wording without adding a partial-qualification algorithm. Challenge retained stub size as a reason to rewrite already-preserved code.
friction: A broad high-effort review of the full sketch history took about nine minutes after the fix was already verified. Tyler called out the delay; use a bounded adjudication of concrete findings and stop expanding this stub task.
