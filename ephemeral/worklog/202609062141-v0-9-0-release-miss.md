friction: Released Tractor from first principles without loading the release-tractor skill, which exists in this repository and governs every version release -> load the release skill before any release work, including when the release is one step in a larger request.
correction: The release-tractor verification gates ran after publishing rather than before. The race suite, vet, golangci-lint, both plugin validators, and the diff check all passed, so nothing shipped broken, but the order was wrong and a failure would have been found too late to matter.
friction: Skills the repository owns were not installed, so no skill surfaced during the work that would have named the release procedure -> install the repository's own skills before working in it.
correction: Diagnosed the Codex marketplace SSH failure as a passphrase problem from a probe that was itself sandboxed, stating one cause with confidence when two were in play. Retried outside the sandbox on request: the sandbox was real and separate, and the passphrase held up only under the unsandboxed retry.
correction: Treated an SSH-only plugin marketplace source as an environment limitation and reached for a per-process git URL rewrite, then handed the remaining steps back as something needing a terminal with a key. The repository is public and clones anonymously over HTTPS, so the source was simply wrong and the fix was to change it. Prefer fixing the configuration that is broken over carrying a workaround that lives nowhere and breaks for the next person.
correction: Read two plugin validator scripts as Python inside a Go project. They are Codex system skills under the Codex home and belong to Codex, which consumes the plugin. The repository's only Python is throwaway proof scripts under the ephemeral tree.
resolution: v0.9.0 is tagged at its merge commit and released, the binary is installed, and the repository's three skills are installed globally. The marketplace source now points at the public HTTPS URL, and the plugin was removed and reinstalled from clean to prove it needs no local git configuration.
