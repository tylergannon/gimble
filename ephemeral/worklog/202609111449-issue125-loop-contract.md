correction: Issue 125 and Tyler's request reject Task{Lap, Text}; counters belong to ordinary Go. Requested task information includes a short name, definition of done, and command or agent validation. This session is recommendation-only; no implementation or commit was requested.
finding: Original sources are ephemeral/legacy/checklist/checklist.go (Item: Name, Check, Command, Infer, Doc, Checklist, Done), ephemeral/legacy/program/loop.go (Iteration: Item, Number, Feedback, Checklist), and ephemeral/legacy/program/workflows/workflows.go (compact nested chapter and delivery workflows). Infer separated evidence paths and a judge question; command and inference could both run.
finding: ephemeral/research/api/API.md:508 deliberately replaced mechanical first-open selection and engine-owned done with planner-directed work. Recovering meaningful task fields does not imply restoring the old selection or completion algorithm.
finding: Current internal/workflows/sprint/sprints.go already counts work locally across multiple loop activations. Its final validator is distinct from the planner saying there is no next task. Current Loop planner prompts omit ScopeText, and task data reaches scope events through a separate string context key.
proposal: Prefer Loop[T Output] yielding complete workflow-owned structured task values, with the ordinary Task type providing the initial Name, Instructions, DefinitionOfDone, and declarative command/query validation shape. Rename Laps to Tasks. Treat genericity as the planner output contract, not a bag of extra metadata or generic execution callbacks.
proposal: A fully generic Loop cannot also discover and execute task validation fields without an additional protocol. Recommend explicit command and validator calls in the workflow, recognizing this as a deliberate change from the current automatic command sweep. Keep validation plans separate from recorded results and planner exhaustion separate from validated completion.
proposal: Persist and yield the same validated task snapshot, publish it in the task's scope, and explicitly supply the completed scope's recorded feedback to the next planner call. Do not assume child scope writes become parent values. Keep runtime scope ordinals separate from planner-authored task names.
correction: Tyler rejects Instructions as encouraging pedantic implementation prescriptions. Prefer prose describing the intended end state and necessary information the worker might not discover independently; trust the worker to choose the method. Detailed procedural direction may signal a task that needs decomposition.
finding: Tyler asks to consider df-promise conceptually, not to set up or run a Promise Loop. Its reusable core is a scoped falsifiable statement with evidence-linked verification; recurring execution, budgets, fingerprints, and badge lifecycle are additional machinery.
proposal: Revise toward Description plus DefinitionOfDone as a list of promises, with validation attached to each claim and observed results stored separately. Preserve an overall-goal check: passing all planner-authored promises does not establish that their set covers the requested goal. Defer an unconstrained generic Loop because it would prevent the runtime from relying on this shared contract.
correction: Tyler distinguishes promises from iteration lists: dispatch responds to completed work, poor work, remaining work and priority, and the overall 90-95 percent completion criterion. The endorsed direction separates promises, dispatch, and completion assessment. The earlier proposal to make every task's definition of done a promise list is superseded; exact types and genericity remain open.
decision: Tyler requests the discussion be written down. Preserved the endorsed direction, practical examples, selected verbatim statements, historical sources, and unresolved API questions in ephemeral/research/api/LOOP.md, linked from API.md. Documentation only; no implementation or commit requested.
correction: Tyler resolves the readiness threshold: every promise must meet "it works and it's 90-95% done" when assessed by a validator agent. This is subjective latitude to release working software, not a percentage of promises that may be satisfied. Deterministic validation remains binary. Updated LOOP.md and preserved the clarification verbatim.
proposal: On Tyler asking whether the Loop contract is ready for re-engineering, added a concrete proposed contract to LOOP.md: fixed structured Task, Tasks iterator, explicit projection of completed scope feedback into the next planner call, workflow-owned command and agent validation, and dispatch exhaustion separate from overall fulfillment. No runtime changes or commits. The proposal explicitly calls out replacing the existing command sweep rather than silently changing that behavior.
decision: Tyler asks to use df-sprint-plan and df-easy-loop-simple as sources for task-sizing language, and to support dispatch from prioritized promises without a sprint plan. Read both skills plus write-prompts and the coherent-boundary guidance in orchestrate-attractor-loops; no orchestration run was requested or launched.
proposal: Size the next assignment as one coherent outcome the intended worker can reasonably complete and demonstrate in a working session, accounting for context and uncertainty. A session is a sizing reference, not a one-turn limit. Sprint plans are optional candidate work; dispatch adapts them or starts directly from prioritized promises. Added this direction and its live-proof implications to LOOP.md.
correction: Tyler sharpens dispatch from next doable work to greatest concrete gain toward the overall Definition of Done: greedy selection toward the goal. An earlier phase can remain roughly 87% complete with a nonblocking defect while later work proceeds. Updated planner language, phase-gating guidance, and planned behavioral evidence; deferred defects remain visible and deterministic failures remain failures.
correction: Tyler asks for a short record of what we are doing and how to know it is done, not a complete sprint-planning process. Stopped before launching drafts; removed the new sprint-planning scaffolding and wrote LOOP-WORK.md with four work outcomes and behavioral completion evidence. Planning-model preflights had succeeded; no drafting or implementation agents were launched.
decision: On Tyler's execute/verify/merge request, implemented a fixed Task rather than a generic Loop: Name, Description, DefinitionOfDone, and nested command/query Validation are the common dispatch contract. The planner's absent next task ends dispatch without claiming fulfillment.
decision: Preserve the editable backlog but validate its immutable goal and structured tasks after every planner turn. The returned task must exactly match a backlog entry; the task snapshot is then recorded in planner and scope events so later edits cannot rewrite history.
decision: Each task scope stores its Task before yielding and renders only its locally recorded values as feedback for the next planner call. Parent ScopeText is rendered independently, which gives the planner current shared context without promoting completed child values.
decision: Validation fields remain data. The sprint workflow now executes the requested command and query visibly, records their actual results with the worker result and repository checks, and leaves failed work uncommitted for the planner to respond to.
friction: A JSON-encoded backlog fixture initially used Go field names because backlog had only YAML tags; immutable-goal validation caught the mismatch. Added matching JSON tags so both durable representations agree.
friction: The first real Luna planner call rejected an optional `next` property because Codex strict structured output requires every property to appear in `required`. Represent dispatch exhaustion as required `next` with a nullable Task, so the portable wire value is explicit `{"next":null}`.
friction: A live planner correctly ended dispatch after a disclosed nonblocking probe failure because the fixture's promises required only the release check and guide. A proof that expects failure-driven follow-up must make later probe stability a real prioritized promise; otherwise forcing another task contradicts the greedy dispatch rule.
friction: After stability became a promise, a Luna worker claimed the probe later passed while the workflow's recorded probe exited 7; the planner trusted the prose and ended dispatch. The planner prompt must state the contract's precedence rule: recorded deterministic results are authoritative and cannot be overridden by agent claims.
finding: With deterministic precedence explicit, Luna selected the retained stability task after seeing exit 7. The five-minute proof deadline then expired during a redundant worker turn for that validation-only task. Let the workflow execute a command-only follow-up directly and reserve the live worker for assignments that require work.
friction: A later run's planner inspected probe.sh before dispatch and consumed its one-time failure, so the task's recorded probe correctly passed and offered no failure to replan from. Reset the disclosed transient marker immediately before the first recorded probe; this controls the observation boundary without prescribing the planner's task sequence.
design_bug: A planner with unrestricted workspace tools can perform the assignment or its validation during its planning turn, making dispatch state incomplete and bypassing task-scope evidence. State the role boundary in the prompt: inspect to plan, change only the backlog, and leave work and validation to the dispatched workflow.
friction: Luna's first accepted planner result wrote an unquoted colon into backlog YAML. Loop refused to yield it but initially ended the run, which was safe but too brittle and regressed issue 115. Invalid backlog revisions now produce a repair turn with the parse error; malformed returned Tasks and backlog disagreement remain terminal errors.
