decision: Independent review of PR 140 at 2a52971 in a fresh detached worktree (.claude/worktrees/claude-loop-review); read-only apart from ephemeral/review/claude-loop-api/. Codex's REVIEW.md read only after own code reading and probes.
friction: Fresh worktree needs `just build` before `go vet`/`go test` (issue 112); ~4 minutes of pnpm/vite before any Go gate can run.
finding: Loop's dual-channel planner protocol (edit YAML file + return JSON Task, exact struct equality required) ends the whole Loop on a YAML block scalar's trailing newline or one trailing space (probes P1, P3), while an unparseable backlog is retried without bound (P2). Root cause is asking for the same fact twice; proposed single structured answer with `next` as an index.
finding: HarnessAdapter has no Close; scope.end only flips a flag, so the ownership promise cannot be kept by the runtime (issue 108 is a symptom). Interrupt is an optional type assertion while Steer is required.
finding: Set/SetJSON can fail only through programmer error; the `if err != nil` around them is 17% (sprints.go) to 30% (Codex's live main.go) of workflow lines. Proposed panic on misuse, Group.Go recovering.
finding: Loop's feedback projection re-renders task-scope keys that only shadow a parent (e.g. `role`), so the planner's second prompt carried both "You plan" and "You implement" (prompts.txt seq 34).
friction: Tool hooks (Write, mark_chapter) timed out twice in this session; bash heredocs used instead. Bash tool timeout caps at 10 minutes; long `tractor run-prompt` turns must run in the background.
decision: Cross-talk with Codex by resuming its Codex Desktop reviewer session (`tractor run-prompt --session 01a09585-487d-7cf3-9983-88a1466c4b40`), one new file per round under ephemeral/review/claude-loop-api/rounds/, log in DISCUSSION.md, joint RECOMMENDATIONS.md at the end.
correction: Codex showed `Set` can fail on real data (math.NaN through the float64 arm) and that the runtime has no panic boundary; panic-on-misuse withdrawn. Lesson: check the type set before claiming "all failures are programmer errors".
correction: My ceremony table mixed all error plumbing with Set-specific plumbing and the percentages used the wrong denominator (Set share is 9% and 22%). Label measurement columns by exactly what they count.
correction: "Skip shadowing keys in Loop feedback" rejected by Codex: key collision cannot distinguish an execution-only shadow (`role`) from an intentional child revision (`status`). Lesson: a runtime heuristic over key names is a hidden semantic; prefer an explicit workflow shape.
decision: Joint recommendations converged in two rounds with no dissent left for Tyler; the consensus transport (tractor run-prompt --session on the Codex reviewer session) worked for three turns at medium effort, 5–8 minutes each.
