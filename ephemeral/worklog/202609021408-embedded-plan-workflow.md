# Embedded plan workflow sprint worklog

decision: planning artifacts use a strict four-line recommendation contract with heading, Size, Rationale, and Next so the runtime handoff is mechanically parseable

decision: checklist item field presence is preserved separately from its boolean value so plan validation rejects agent-authored done: false as well as done: true
