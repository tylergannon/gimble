decision: Issue 129 is implemented on top of merged PR 127 so the worker-foreground cancel-and-join path remains the lifecycle owner.
decision: Supervision retains a private 256 KiB recent-event view and assembles each look within 64 KiB; the existing 2,000-byte per-event shortening remains the individual-entry ceiling. These are runtime constants, not workflow controls.
decision: A supervisor that falls behind receives an explicit local activity-item interval and the durable worker session-log path; the prompt does not pretend a truncated interval is complete.
decision: Calibration supported the internal budgets: 10,000 fixed-size events reached a 262,086-byte, 2,299-entry peak by event 3,000 and neither measure grew through event 10,000; the maximal synthetic first look was 62,325 of 65,536 bytes. A live gpt-5.6-luna worker and Haiku reviewer crossed gap 1..372 with a 61,016-byte look, the reviewer issued the requested runtime-boundary objection, and the worker incorporated it while the durable raw worker log reached 1,832,890 bytes.
