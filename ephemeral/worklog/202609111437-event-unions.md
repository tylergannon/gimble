correction: Issue #122 must define distinct partial agent variants such as message, thinking, and tool deltas; a generic Delta event would repeat the comprehensive-event design bug.
decision: Issue #122 defines the complete known event vocabulary; issue #117 remains responsible for teaching adapters to emit variants they do not emit today.
friction: The rebased baseline could not run go test ./... because web/dist.go embedded an absent build directory; upgrading both SKGO dependencies to v0.3.7 and adopting its tracked web/build/.gitkeep scaffold restored bare-checkout compilation.
doc_bug: PR #123 says a tracked placeholder lets a bare checkout compile, but its final web rollback removed internal/webembed/bare.txt and restored web/dist.go with no tracked web/build file -> preserve a tracked placeholder under the actual embedded directory and prove compilation with generated build output absent.
friction: TestAttestEventFixture synchronized with an arbitrary sleep and identified reviewer turns by racy native-session ordinals; synchronize on the reviewer prompt and retain the first reviewer interval so repeated looks cannot overwrite the overlap proof.
decision: Polytype's closed grammar cannot project json.RawMessage or recursive arbitrary JSON; opaque provider payloads are explicit JSONText strings, keeping event records projectable and lossless without weakening the union schema.
decision: Polytype's closed grammar cannot project json.RawMessage, and a provider-template probe left unresolved schema holes when the raw value was nested in a declared record -> represent opaque provider payloads as JSONText strings so every event remains projectable and round-trippable.
friction: TestAttestEventFixture used a fixed sleep before steering, so it could assert overlap before the reviewer actually started -> synchronize on the reviewer's first turn instead of wall-clock timing.
correction: Lifecycle events and both persisted record envelopes are part of the event model owned by issue #122; moving the reader internal in #123 did not authorize making those types private. Export every union variant and record type, and prove they are usable from package gimble_test.
