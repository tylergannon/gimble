# Issue 32 worklog

decision: When a previously done item regresses, normal first-open selection applies after reconciliation; the next selected frame inherits the complete failure block so the body can see every failed item and log path.
