friction: synchronous fsync on every event changed supervisor timing and caused the existing timing-sensitive test to miss its steer -> keep the single-writer mutex and append atomically without forcing a disk sync per event
decision: extend the existing harness Event payload with durable placement and lifecycle fields so adapter callbacks remain compatible while runtime events share one JSONL representation
