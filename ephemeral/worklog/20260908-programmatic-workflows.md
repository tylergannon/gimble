# Programmatic workflow research

decision: Tyler requests lower-model research and a question-specific semantic index before recommendation or POC. Declarative goals remain; exact serialized resume is not a product priority.
friction: public browser and raw GitHub fetch returned 404 for diffusioninc/skills; authenticated gh contents API succeeded. Use authenticated collection for that source without inferring that the project disappeared.
decision: Agent research artifacts and selective upstream snapshots will live in ephemeral/projects/tractor/programmatic-workflows; no production code changes or docs/ writes.
correction: Root verification found existing public HarnessBackend.RunResult and run-prompt generic-result use beyond the lower-model initial source selection. The fixed next/notes schema is the graph pipeline boundary, not a universal native harness limitation. Research and POC scope must account for this cheaper seam.
correction: Current resume rebuilds loop frames from ledgers and can rewind; graph authoring is not equivalent to full program-state serialization, even in today's Tractor.
friction: Upstream Go snapshots under a tracked ephemeral corpus would be included in future Go package discovery -> store copied Go source as .go.txt and retain original paths in manifests.
decision: Agent recommendation is a bounded ordinary-Go routing/typed-wrapper experiment over existing RunResult, with declarative goals and a source-linked structural preview. Keep graph workflows as control/compatibility until authoring and human comprehension gains are demonstrated. A lower-model builder counterproposal remains documented, conditional on complete static topology being required.
correction: Unknown runtime values do not prevent drawing both syntactic branches or lexical loops before execution. They prevent predicting the actual path/instances. Root corrected multiple researchers' stronger visualization claims; graph definitions also cannot predict actual agent outcomes.
friction: Fresh retrieval reader reported consulting expected-target evals after retrieval despite the no-target instruction -> record the deviation and treat its eight answers only as a source-match smoke check, not a blinded quality or efficiency benchmark.
correction: Tyler challenged the assumption of per-project workflow invention and proposed declarative work consumed by a small family of Tractor-owned routines. Reframe analysis and visualization around known routines and useful diagnostics; lack of semantic proof is not a reason to dismiss a missing-validation warning.
decision: Agent reconsideration favors testing one maintained routine on two different tasks, plus one useful custom-routine analyzer warning. This is a revised proposal, not authorization to build or revive the earlier canned size-routing implementation.
decision: Tyler makes runtime observer visibility central: current work, work history, and time/token attribution. Record feasibility as structural routine model plus per-invocation runtime observations; defer implementation. Static shape alone cannot account for repeated visits, parallel activity, or actual usage.
correction: Tyler specifies program-owned plain JSON task input with published schema, replacing graph-language schema publication as the primary concept. Validation may be code, an agent or hybrid. Do not invent a universal Work/validation DSL; distinguish input schema from orchestration structure and per-call result schema.
decision: Tyler proposes an eval tier: fixed example project and established requirements/validations, multiple orchestration shapes, observed time/tokens/outcomes. One routine on two tasks tests reuse, not this comparison. Revised handoff retains both axes and labels the next experiment unratified.
correction: Tyler wants program-flow projection with respect to specific calls; lexical nested loops and scoped supervisor relationships are relevant even without complete arbitrary-program understanding. Known routine visualization is a useful case, not a restriction to a canned catalog.
correction: Warnings flag known bad shapes, not correctness. Go linter tricks are useful but secondary. Names such as Big/Micro were illustrative and never a proposed taxonomy; drop warnings against an unproposed naming convention and other obvious caveats.
decision: At Tyler's request, TIE-OFF.md captures the full conversational journey, all twelve latest corrections, source inventory, open questions, and continuation boundaries; older proposals now point to it so compaction does not restore superseded assumptions.
friction: copied upstream Go snapshots under corpus would participate in broad `go test ./...` discovery -> preserve bytes as `.go.txt`, retain original `.go` paths in the source manifest, and cite the inert local paths.
decision: Tyler authorized a narrow Go implementation in this worktree, aiming for 30 minutes with a 90-minute ceiling. Implement codergen, validation, command execution, and iter.Seq2 over existing checklist ledgers; derive primitives from built-in workflows and defer fan-out/fan-in.
decision: For this POC the iterator owns validation and done reconciliation, while its Go body owns implementation/review routing. MaxIterations bounds body yields and must still permit final post-body validation; break does not imply completion.
friction: First native Go-program run rejected a nonempty ThreadKey with FidelityNone. A fake backend accepted the invalid turn -> validate constructed AgentTurn against the existing harness contract in focused tests as well as running native proof.
friction: Native harness RunResult appends to a preallocated log segment without O_CREATE. Directory creation alone was not the missing step -> allocate the uniquely named segment before starting the turn; the run-prompt allocator was the existing precedent.
decision: Narrow POC is live-demonstrated with flat sprint execution and nested chapter/sprint execution. Delivery planning is implemented but not live-proven; fan-out, supervisors, session continuity and visual mapping remain outside this slice.
correction: Repeated operations can share a human-readable name. Native usage summaries must take the final cumulative event per invocation/log file, then aggregate; grouping by operation name drops repeated calls, and summing every cumulative event double-counts.
correction: Tyler pointed out that input validation and inline prompts obscured the Go algorithm. Separate invocation/schema validation, configuration preparation, prompt construction, and typed agent adapters from workflow control flow; keep loops and review-routing decisions visible in workflows.go.
decision: Tyler wants critique-circle and bake-off pseudocode to drive the concurrency API. Prefer ordinary goroutines/errgroup; proposal records join boundaries, typed critique/trial outcomes, and scope inheritance before adding a Future or new graph constructs.
decision: Tyler states the central architectural claim: workflow shorthand tends toward a pseudo-programming language, so author workflows as Go programs. Treat scoped context as agent-facing working state and an evolving addition to the semantic index root; every request includes current context, the root index, and retrieval instructions, while larger supporting material lives in ordinary searchable files. Preserve his original verbatim beside an explicitly edited documentation version.
friction: Current sequential POC cancels with Backend.InterruptAll and reuses human-readable names as native binding keys -> parallel execution needs per-call cancellation and identity plus branch-local prompt context, not just wrapping existing calls in group.Go.
correction: Tyler rejects the runtime-context versus semantic-index overflow dichotomy. Design one agent information environment from the outset: goals, rules, requirements, available means, scoped state, index, and on-disk material. The first message should maximize objective clarity and provide useful paths to needed detail, even when the detail would fit inline.
correction: The program metaphor serves agent success, not completeness of the metaphor. Context engineering includes distributing knowledge and responsibility across agents and time: builder, concurrent supervisor, subsequent checker. Reframe docs around accuracy, time, token/cost, and a manageable responsibility for each agent; preserve the clarification verbatim alongside the original statement.
decision: Tyler frames Tractor's direction as a response to experience using Attractor-style authoring and agents' difficulty designing effective workflows. Help humans and agents understand the context-engineering consequences of a shape. Put this in broader direction documentation, not only the Go-program document.
correction: Preserve the real strength of a good graph: readers can understand stages and progression. Roughly one page of Go should convey the method like pseudocode; judge names, composition, DRYness, indirection, library functions, and authoring guidance by that comprehension goal rather than brevity or abstraction alone.
decision: Tyler wants future diagnosis of failed, slow, or repetitive runs using initial and actual requests, semantic index and indexed material quality, steering frequency and nature, steps, time, and tokens. Capture this as direction without enlarging the first implementation; static diagrams remain a feasible partial projection to investigate, while runtime observation remains important independently.
decision: Tyler accepts static extraction of operations and inferable flow combined with an agent session for conceptual connections and spatial layout. The purpose is human understanding; the derived diagram need not be a complete execution graph or determine what Go may express.
decision: Tyler proposes a small action vocabulary (command, agent task, supervisor/coach) with five/seven as possible authoring budgets and meaningful distinctions such as coding, validation, research, and indexing. Preserve role visibility without requiring a distinct runtime node type for each purpose; exact taxonomy remains open.
correction: Research and indexing should prepare local, searchable task knowledge as part of substantial workflows. Briefly revisiting the collected method confirms question-oriented routes to actual local sources; incorporate collection/navigation and explicit knowledge gaps into the unified context, not as an afterthought. New findings can trigger targeted research/index updates during the work.
decision: Tyler makes the separation of program shape from starting input a high-level principle. Input includes the available data and the index's shape and quality; data may grow and the index may evolve during execution. Preserve method versus task/information conditions as separate things to author, understand, reuse, and vary in evals.
