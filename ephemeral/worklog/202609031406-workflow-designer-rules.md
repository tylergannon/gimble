# Worklog: workflow-designer rules distillation (2026-09-03)

decision: the rules document for the interactive workflow designer lives at ephemeral/projects/tractor/workflow-designer/rules.md (branch worktree-workflow-designer); AGENTS.md points at it; memory pointer `workflow-designer-rules`. Source: Tyler asked for the 10–15 most important planning/design/execution rules mined from memory, ephemeral notes, ~/.claude/skills, and reference clones.
doc_bug: MEMORY.md index line for living-instructions-initiative told sessions to read ephemeral/projects/tractor/living-instructions/{decisions.md,loop-node.md}, which 3f83f2c deleted -> fixed in memory: recoverable via `git show 480b6dd:ephemeral/projects/tractor/living-instructions/decisions.md` or origin/worktree-goal-gates.
friction: sandboxed Bash in a worktree session refuses `cd` to the shared checkout, compound commands that touch git, and (per subagents) shell loops, shell variables, and any path containing "eval" (inspiration/gbrain-evals/eval) -> run plain git commands from the worktree; use the Write/Read tools or literal paths.
friction: agent-tooling-research expects the reference repos as submodules under ~/src/agents/submodules/<name>/, which are empty locally -> shallow-cloned them into inspiration/ (excluded via .git/info/exclude); `git submodule update --init` in the agents repo would restore the intended layout.
skill_issue: update-borrowed-skills source=mattpocock/skills severity=nit -> borrowed-skills.json pins writing-great-skills; upstream HEAD renamed it writing-for-agents.
friction: notes untracked in the main checkout (ephemeral/projects/tractor/{loop-frames,adoption-design,poem-critique-demo}) and uncommitted edits to issues-32-37/ISSUE-{32,33,35}.md are absent from this worktree -> copy or commit them before designer work that needs loop-frames/memo.md.
correction: decisions 37–60 of the living-instructions record were agent-written (Claude, Sep 2 evening and overnight); a first synthesis cited them as Tyler's decisions and inverted his positions on batched interview questions, on what the agent may decide alone, and on chapters versus sprints -> rules.md rebuilt from the primary records, each rule marked Tyler / agent accepted / agent unreviewed.
decision: every recorded decision says whether a human or an agent made it (Tyler). Whether the human accepted it, and the form of the marks, are an agent proposal. Memory: decision-attribution.
doc_bug: decisions.md numbering differs by version: at 480b6dd d37 = promises, at cbc0686 d37 = review ceiling and d8 carries the twelve-sprint floor -> cite decisions by content, never by number.
doc_bug: memory living-instructions-initiative said "ask one question at a time" and "d37–59 were the misread" -> corrected: the planner's interview batches related questions with recommendations; the misread was the canned graphs (d30–35, Claude alone) plus d51 and d56.
friction: transcript timestamps are UTC and Tyler's local time is UTC-6 (git author offset) -> convert before dating a decision.
friction: subagent summaries of the record labeled Tyler's decisions "the misread" -> for provenance questions read the primary records directly.
correction: tracked files record what matters in plain words with a human-or-agent mark; no verbatim transcript quotes, no message timestamps, no notes about individuals -> rules.md rewritten in that form; the branch squashed to one commit before any push.
decision: rules.md carries a dated status section: no design for the designer exists yet; open whether the library of named workflows and the shipped content library survive the Sep 3 cut.
