correction: Issue 126 review: current root Godoc exposes no channel parameters or results. runDone, turnDone, readDone, and readErr are test plumbing. Historical public ReadRun is now internal/runlog.Read; use current source rather than Sprint 2 memory for API claims.
decision: Review recommendation, not an approved implementation: retain blocking Run/Generate, context cancellation, Group.Go/Wait, and synchronous callback observation. Use existing joining primitives for completion errors; retain local channels where arrival order is the workflow's actual data flow.
friction: TestAttestEventFixture returns on a Steer error before receiving turnDone; a buffered error send prevents a blocked sender but does not establish joining on that path.
doc_bug: supervise.go waits for supervisor Generate calls that share the worker's incoming context; worker completion closes done but does not cancel active supervisor calls. The documented no-hold-up behavior needs cancellation of owned supervisor work followed by joining, with the worker result authoritative.
decision: A reader following the final complete record must be owned outside the run body: Run writes complete only after the body returns. Observation cancellation and read errors are separate from the run's terminal result.
decision: Tyler requested a PR for review and feedback; publish the implementation and evidence, leave the PR open, and do not merge.
evidence: The new success/failure regression failed before the runtime fix because both supervisor calls waited for the parent deadline. Foreground worker execution with deferred supervisor cancellation and joining passes both cases.
friction: Repeated race runs exposed the fixture overwriting its first overlapping reviewer interval with a later look. Keep the fixture reviewer active until worker completion, establishing the overlap with a readiness signal instead of a sleep.
evidence: Live run 20260911-145304.join used Codex gpt-5.6-luna and Claude Haiku. Worker returned DONE at 20:53:19.625207Z; reviewer ended interrupted with context canceled at 20:53:20.249375Z; Run returned nil with the parent still active after 15.858s. Reproduction and original lifecycle log are in ephemeral/attest/issue126.
correction: TestSupervise assumed exactly one look, but fake.Steer emits a new user event that legitimately triggers another look. Assert the worker runs once and its tool result is consumed exactly once across all looks, preserving the delta-stream claim without a scheduler-dependent look count.
decision: Tyler authorized rebasing onto latest main, force pushing, and merging PR #127. Resolve the event-union test conflicts by preserving LifecycleRecord and AgentEvent while retaining explicit joins and the active-reviewer readiness signal.
friction: A failed conflict-edit script did not stop its shell, allowing rebase continuation with conflict markers; use fail-fast shells for dependent edit and commit steps and validate the result before publishing.
friction: The first post-rebase web tests saw the previous embedded frontend adapter. Complete just build before testing the runtime after a skgo dependency update; regenerated assets then passed the full race suite.
evidence: Rebased content passed build, vet, full race suite, and ten repetitions of the supervision/attest/reader/group tests. Live run 20260911-154728.join used Codex gpt-5.6-luna and Claude Haiku: DONE, reviewer context canceled, Run nil and parent nil after 11.336s. Original rebased lifecycle evidence is ephemeral/attest/issue126/rebased-run.jsonl.
