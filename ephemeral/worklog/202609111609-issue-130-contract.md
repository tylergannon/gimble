# Issue 130 contract research

correction: Tyler requires existing upstream agent-event solutions to be investigated by smaller agents; manager owns API decisions, reducers remain delegated implementation work.
decision: Rebased codex/issue-130-contract onto origin/main at 607a6e1 before research. Main now includes sealed event unions (#128), runtime web serving (#132), and supervisor joining (#127).
decision: Source checkouts and disposable probes live in ephemeral/inspiration, excluded through local Git info/exclude at Tyler's explicit request; reusable reports and contract live in ephemeral/research/issue-130.
doc_bug: Historical Sprint 3 replay/tail description is superseded for issue 130 by current snapshot plus incremental stream requirement.

correction: Tyler explicitly chooses reset on every reconnect: the first stream object is a complete latest snapshot replacing client state. No cursor recovery or retained replay buffer. Atomic snapshot capture and subscription remain required to avoid a handoff gap. This supersedes issue #130's optional cursor recommendation.

decision: Adopt OpenCode-style identified part upserts and field deltas for internal Go/TypeScript read-model reducers; keep provider normalization at the adapter/runtime boundary.
decision: Native JSON SSE plus EventSource with a first-frame replacement snapshot on every connection. Kit 3.0.0-next.27 SharedIterator drops intermediate pending values, reproduced by the transport agent; query.live is unsuitable for exact deltas.
decision: Retain skgo devalue for SSR; generated JSON for dedicated SSE. Compression remains ordinary HTTP behavior, not a gzipped application object.
friction: Initial research conflated model-call usage with turn usage and illustrative ports with upstream execution -> manager requested corrected source semantics and honest proof labels before adopting findings.
decision: Live Haiku raw frames prove nested message.id connects streaming/full content and nested tool_result.tool_use_id connects results; pinned Go SDK loses the latter, so adapter parse boundary must preserve it instead of FIFO inference.

correction: Tyler explicitly selects direct OpenCode session.* schema plus matching JS reducer as the preferred contract. Must trace and run native producer -> transport -> applyV2 -> projectV2 -> rendered UI, distinguishing direct V2 rendering from legacy projection and real native events from protocol:v2 mocks. Prior custom PartUpdated vocabulary is superseded where upstream serves the requirement.
decision: Pinned UI trace finds `data.session_message` is the direct V2 source, but timeline components still render projected legacy `data.message`/`data.part`; preserve this seam in any Go port until component migration is explicitly requested.
doc_bug: Checked-in `packages/schema/src/session-event.ts` uses `session.next.*`, while the app's vendored `@opencode-ai/client` 1.17.13 generated types and reducer use `session.*`; schema selection must follow the vendored client/reducer pair at 95daf906, not the unrelated checked-in vocabulary.
decision: V2 reducer private pending admissions are not represented in the public `createServerSession` state or snapshot API; a snapshot-first implementation must explicitly define how `session.input.admitted` state is seeded before later promotion.
decision: GitHub PR map identifies #37023 (closed) as the strongest direct-V2 timeline component lead; #40376 and #41930 provide real browser/lifecycle coverage but their timeline payloads are fixture/mock-server driven.
doc_bug: At pinned checkout 95daf906, checked-in producer schema declares `session.next.*` while vendored app client/reducer consume `session.*`; PR #35217 documents the rename but this pin's schema/client artifacts are inconsistent. Native SSE must be captured before claiming producer-consumer compatibility.
decision: Active producer proof confirms `/api/event` uses `@opencode-ai/protocol/groups/event` built from `EventManifest.ServerDefinitions`, and `publish-llm-event.ts` publishes `SessionEvent` directly; no event-name alias layer exists. At this pin it emits `session.next.*`, so protocol:v2 tests using `message.*` or reducer-only `session.*` fixtures cannot prove native app delivery.
decision: Gemini's `origin/v2` tip c55ee2a is the newer merged native path: `client/src/solid/data.ts` and `session-ui/src/timeline/projection.ts` consume/render `SessionMessageInfo` directly with `session.*`; migration PRs #38818, #40374, #40376, #40378, #40381, #40608, and #42999 are merged. Closed #37023 is superseded as the latest direct-render lead.

correction: Tyler questions whether real V2 event streams have ever reached the UI; requires branch and PR/draft investigation and explicit assessment of semantic inference. A protocol:v2 label and direct reducer unit test are not end-to-end evidence.

## Native V2 research correction

The newer official v2 checkout at c55ee2a8152603f04a409163bd3edf79c425fbd7
contains a direct native UI; older dev projection findings cannot establish
its behavior. The schema and updater must be pinned together. The new
updater is createData with API/cache effects, not the older pure reducer.

Researcher disagreement required direct inspection: session-event.ts requires
text/reasoning ordinal, but data.ts editText/editReasoning selects the latest
matching part and never receives that ordinal. Neither 'no ordinal in schema'
nor 'updater honors ordinal' was correct. Require schema-decoded multi-part
fixtures; preserve any mismatch rather than silently changing the oracle.

The first 'protocol:v2' browser pass used legacy message payloads. Sol reran
actual session.* fixtures through the direct newer browser path and separated
controlled producer evidence from live-backend evidence. A casted object or
passing fixture label is not proof of native schema acceptance. Keep exact
validated payloads and name what was mocked in the report.

No commits; source checkouts and disposable runtime probes remain locally
ignored under ephemeral/inspiration. The contract now replaces the custom
content vocabulary with the same-revision native borrowing boundary.

The ordinal concern has a concrete upstream producer guard: text and reasoning
instantiate fragments with single=true, rejecting another start before end.
Schema-valid overlap tests are outside that producer invariant. Borrow the
invariant, not an invented ordinal fix. The eventual native backend probe
succeeded using existing opencode-drive@2.1.0: simulated model, real backend,
SSE, and shell. Browser and backend captures remain separate proof stages.

Main advanced during research to f79a864903f1387ea9e3b55f098340a828c1a483.
Rebased the research branch again successfully; only uncommitted research and
worklog material remains. No production Go port is claimed by this task.

## Unnumbered sprint artifact

correction: Tyler requested the sprint artifact without sprint numbering or
process overhead. The numbered ledger/index/interview workflow was stopped;
one unnumbered SPRINT.md uses the existing research. No draft fan-out or
three-model consensus is claimed. CLI/model preflight had already succeeded.

decision: Infrastructure completion has two levels of evidence. Contract-wide
conformance and snapshot-future equivalence prove the reducer milestone;
production-path observation, live adapters and resource bounds additionally
prove the full issue130 sprint. Separate upstream backend/browser probes do
not establish that final Gimble integration claim.

correction: Tyler flagged excessive phase counts and repeated verification/review
machinery. Consolidated six phases into three actual dependency boundaries:
paired reducers, runtime observation, consumer proof. Setup and verification
are part of implementation sessions. One final independent evidence review;
retain valid evidence and rerun only affected claims after changes. Worker
assignments are bounded by code surface and can run in parallel; context
continuations do not automatically create another phase or review cycle.

 decision: User authorized executing the unnumbered three-phase sprint with Opus and gpt-5.6-sol workers; overrides Fable/Astra skill defaults. No commits remains binding. Reuse proof and one final independent review.

correction: Execution source inventory found settleMs only serves MCP cache; no session debounce belongs in the borrowed reducer. Public snapshot must carry portable read continuation (including deferred rename action) to restore mid-read.

correction: Codex tokenUsage.updated cannot identify model calls: rate-limit and compaction-estimate paths emit it. The pinned source already exposes rawResponse/completed with response ID and per-response usage; use that existing boundary.
decision: Upstream zero defaults for unknown cost/token fields need explicit outer availability/provenance; UI must show unavailable rather than claim observed zero. Do not assign Claude whole-turn cost to each model response.
friction: CLI worker cannot receive live collaboration messages; binding interface updates in a file were not promptly read. Pause/resume the same worker with explicit interface steering rather than let an incompatible implementation continue. Keep common JSON interface settled before large parallel ports.

## User correction: proof volume and Git footprint

User flagged about250k Git-visible changed lines and possible downloaded-source leak.
Main inspiration/oracle checkouts were ignored, but CLI logs contributed162k lines
and SDK archive scratch downloads46k. Locally excluded execution/*.log and moved
SDK scratch to ignored ephemeral/inspiration/claude-sdk-scratch. Remaining~65k
untracked lines include42k of actual patched SDK source/tests. Keep raw output
ignored and small runners/provenance reviewable. Paused100k browser stress campaign;
replace with focused history/queue measurement. Don't turn building-block proof
into a second product or repeated validation ceremony.
