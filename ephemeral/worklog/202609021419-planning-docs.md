# Planning workflow documentation worklog

decision: Treat `tractor workflow list` as the authority for runnable built-in workflows. A MEDIUM or LARGE recommendation names its fixed future handoff, but callers must not invoke it until that workflow is listed.
doc_bug: The loops guide presented milestone execution as requiring no upfront plan and an optional planner node, contradicting the new built-in-plan entry point -> make the approved planning brief authoritative while milestone laps re-plan only the next runnable step.
doc_bug: llms.txt named the recommendation sizes without their boundaries -> keep the compact agent-facing surface complete enough to follow the handoff.
