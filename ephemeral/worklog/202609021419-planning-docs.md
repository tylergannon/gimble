# Planning workflow documentation worklog

decision: Treat `tractor workflow list` as the authority for runnable built-in workflows. A MEDIUM or LARGE recommendation names its fixed future handoff, but callers must not invoke it until that workflow is listed.
