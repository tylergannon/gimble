# Live plan-to-MEDIUM proof worklog

friction: the first live acceptance reached PipelineCompleted with two passing committed sprints, but the evidence inspector treated its phase label `execution` as the checkpoint thread key -> map the public proof phase explicitly to the embedded workflow's `medium` thread key
