discovery: Claude Code 2.1.252 accepts `claude-fable-5-1` and reports that canonical model in a no-persistence live turn, despite emitting an `unrecognized_model` catalog warning.
discovery: On 2026-09-01, Claude Code's unversioned `fable` alias still executed `claude-fable-5`; Tractor must resolve its maintained alias before invoking Claude if it is to make Fable 5.1 the default immediately.
decision: Tractor aliases are `fable` -> `claude-fable-5-1`, `fable-5.1` -> `claude-fable-5-1`, and `fable-5` -> `claude-fable-5`; raw provider-native IDs remain unchanged.
proof: A freshly built `cmd/agent` detected a Codex caller, resolved its unversioned Fable selection to `claude-fable-5-1` (named in Claude Code's SDK warning), completed a real tool-backed turn, and wrote the exact bytes `FABLE_5_1_READY\n`; local evidence is summarized in `ephemeral/projects/tractor/fable-5-1-proof.md`.
