decision: Preserve the external acceptance semantics for prereleases: patch finalizes the current core; minor finalizes when patch is zero; major finalizes when minor and patch are zero, otherwise the requested higher component advances.
decision: Use math/big for core components so valid SemVer numeric identifiers are not rejected merely because they exceed machine integer sizes.
