# Retrieval queries

Use only index/README.md as the entrypoint. Follow topic routes to leaves and open cited source. For each question record route, source anchor, short evidence-based answer, file reads, and whether the evidence actually answers the question. These are research retrieval checks, not software tests or comparative agent-performance measurements.

1. Which current agent orchestrator intentionally lets ordinary language control flow drive routing, and how does it recover?
2. Can Tractor currently let each Codergen call choose an arbitrary result type? Where is the constraint imposed?
3. Does writing a Temporal workflow in Go mean arbitrary Go operations are safe inside its workflow body?
4. Can a LangGraph functional workflow produce the same pre-run graph as its graph API?
5. Which Go AI framework has per-flow input/output typing, and what does that not guarantee?
6. What does Dagger's graph represent, and is it a complete pre-execution map of an arbitrary Go program?
7. How could Tractor recover useful progress without reconstructing its stack or replaying an identical trace?
8. Which workflow properties would the Go compiler check, and which validation/routing policies would remain Tractor's responsibility?
9. What happens conceptually if an external side effect succeeds and the process dies before recording it?
10. Can a Go source control-flow graph by itself establish all possible agent-driven workflow behavior?
11. What is the current public/internal library boundary in Tractor?
12. Which source is closest to declarative goal plus imperative reconciliation, and what caveat matters for coding agents?
