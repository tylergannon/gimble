Okay one more thing that I think is really important to include in our high level framing. &#x20;

A well designed workflow should have a really nice distinct separation between the shape of the program and the input data that is provided to start it, which includes the available data (which can grow during execution) and the quality and shape of the index (which can evolve during execution).
