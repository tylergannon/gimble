1. `declaration.md` promises a general planner: “Given a declarative description of some software … [it] decomposes the work into vertical slices … [and] designs a proof for each promise that a coder cannot satisfy while the promise is false.” But `validation/P10/design.md` fixes both end-to-end seeds before implementation—“fixed at commit `6dec5dc…`”—and explicitly leaves unproven “Generality beyond seeds of this size” and “Behaviour the seed’s prose names but its examples do not exercise.” `validation/P3/design.md` also leaves unproven “That the designs are good.” Thus a planner specialized to the two known CLI fixtures can satisfy the complete validation package without demonstrating the declared product’s core ability to produce sound plans for a new software description. I would not build from this until the promise set either requires an unseen representative end-to-end seed or narrows the declaration to the demonstrated domain. Owning node: `brief`.

ROUTE: fail
