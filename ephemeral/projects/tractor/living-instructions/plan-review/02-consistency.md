# Pass 2: consistency

Question: across `declaration.md`, `planning-workflow.md`, `decisions.md`
(37 onward), `chapters.md`, and every `CHAPTER.md`, `sprints.md`, and
`SPRINT-*.md` under chapters 04 to 06, is any concept called by two
names, or any rule stated twice with drift between the statements?
Quote both statements for each. Plans die of internal contradiction
more than of missing detail. Any contradiction is a fail; name the
document that should change and the owning node (`brief` for the
declaration, `decompose` for chapter and sprint docs, `design` for
validation designs).
