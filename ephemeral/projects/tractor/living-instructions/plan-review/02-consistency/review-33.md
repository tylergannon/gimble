1. `declaration.md` defines each promise and its table without routing columns: “Each promise: the statement, what it must not imply, the archetype … and the verifier,” with headers “`# | Promise | Must not imply | Archetype | Verifier`.” But `planning-workflow.md` requires “the promise table’s `Item` column” and says “`verify` selects its designs by the `Chapter` column.” The same promise-table schema therefore both lacks and requires `Item`/`Chapter`. Change `declaration.md` to include those routing columns. Owning node: `brief`.

ROUTE: fail
