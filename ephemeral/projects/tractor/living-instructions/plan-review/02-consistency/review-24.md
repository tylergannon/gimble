- **HIGH — The v2 handoff is incompatible with the mandatory runtime validator.** The new recommendation template emits `Why`, `Package`, and `Review` fields ([recommendation.md](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/content/templates/recommendation.md:1>)), while `ParseRecommendation` requires exactly four non-empty lines containing `Size`, `Rationale`, and `Next` ([artifacts.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/workflow/artifacts.go:203)). Every completed plan is passed through that parser before its handoff is printed ([workflow.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/cmd/tractor/workflow.go:145)). Chapter 5 assigns `assemble` to write the new artifact but contains no validator-contract migration ([sprints.md](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/05-planner/sprints.md:47>)). As written, a conforming v2 plan finishes its graph and then fails at the CLI boundary.

- **HIGH — `replan` can rewrite the evidence gate immediately before the engine attests it.** The proposed order is `implement → replan → sprints`, and `replan` may edit all open items while claiming to read “the last validation” ([planning-workflow.md](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/planning-workflow.md:200>)). The engine does not validate the current item until the subsequent arrival at `sprints`; it reloads the potentially rewritten item and runs its then-current gates ([loop.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/loop.go:73)). Thus `replan` sees stale validation and may weaken or replace the current item’s `check`, `command`, or `infer` before marking—directly violating decision 37’s gate-to-attestation relationship ([decisions.md](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/decisions.md:157>)).

- **HIGH — A failed chapter verification has no repair path.** `verify` runs only after the sprint ledger is exhausted and routes failure back to `sprints` ([planning-workflow.md](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/planning-workflow.md:214>)). An exhausted loop immediately follows `on_done` without dispatching `implement` ([loop.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/loop.go:110)). Consequently, failure cycles `verify → sprints → verify`; `replan` is unreachable because it follows only `implement`. The contract needs a failure-side repair node that appends a new open sprint or escalates to the human before re-entering `sprints`.

- **HIGH — The persisted package lacks an unambiguous promise-to-execution mapping.** Decomposition permits promises and slices to serve one another generally, but validation later says it fills “the sprint item” for each promise without defining identity or cardinality ([planning-workflow.md](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/planning-workflow.md:110>)). The promise skeleton records only one chapter and no sprint/item identifiers ([promises.md](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/content/templates/promises.md:15>)); the sprint skeleton likewise has no promise-ID field ([SPRINT.md](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/content/templates/SPRINT.md:1>)). A chapter verifier therefore cannot deterministically select the validation designs it must execute, and the package cannot enforce decision 37’s rule that a checklist item’s `check` is the promise whose gates it carries.

ROUTE: REVISE


