1. `chapters/05-planner/CHAPTER.md` should change — owning node: `decompose`. `declaration.md` says: “Chapter 5 and 6 items are backlog numbers until the `plan` node turns the sketch into items, when the names replace them,” and routes P8 through “backlog 11 and 13.” The chapter doc instead calls them “sprints 11 and 13 and every sprint that adds a node.” The ledger is currently empty, so these are backlog entries, not numbered sprints; after materialization, names replace those numbers.

2. `decisions.md` should change — owning node: `brief`. Decision 53 says: “Every prompt, supervisor brief, review pass, doctrine excerpt, and artifact skeleton … is a file under `workflow/library/`; no prompt lives in a Go string.” The canonical promise in `declaration.md` instead says: “Every prompt body, doctrine page, supervisor brief, pass, and skeleton is a library file.” `planning-workflow.md` preserves the important `prompt body` distinction by saying “frames and `$goal` are the engine’s, added at run time.” Decision 53 therefore uses both different names (`doctrine excerpt`/`doctrine page`) and a broader rule (`prompt`/`prompt body`) that incorrectly absorbs engine-owned framing.

ROUTE: fail
