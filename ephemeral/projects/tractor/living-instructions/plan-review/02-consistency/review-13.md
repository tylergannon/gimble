1. `max_visits` has contradictory outcomes. `planning-workflow.md:89-96` says: “its exhaustion edge leads to `ceiling` … so the ceiling is a human gate, not a failed run.” Decision 50 likewise calls `ceiling` “the brief/research loop’s exhaustion gate.” But `validation/P2/design.md:109` says: “Exhaustion of `max_visits` needs no check: it fails the run.” Change `validation/P2/design.md`; owning node: `design`.

2. Holdout eligibility is stated both conditionally and unconditionally. `planning-workflow.md:129-132` limits creation to “a universal promise over a set too large to check whole,” and decision 42 says “a universal promise over a set the verifier checks exhaustively needs none.” `chapters/05-planner/sprints.md:39-41` instead requires that “for a universal promise, a holdout” exists. Change `chapters/05-planner/sprints.md`; owning node: `decompose`.

3. The node universe for `workflow show` has two definitions. `chapters/04-library/SPRINT-02.md:49-52` says its output is compared with “the set the workflow YAML declares.” `validation/P8/design.md:40-43` instead uses “the node census of each graph as `Build` returns it (`g.Nodes`, synthesized branch nodes included)” and explicitly says “the YAML is not the universe.” Change `chapters/04-library/SPRINT-02.md`; owning node: `decompose`.

4. The `show` ordering and type contract drifts. `chapters/04-library/SPRINT-02.md:19-24` requires every node “in file order” with `== <node id> (<type>)`, and lines 49-51 require matching `id`/`type` pairs. `validation/P8/design.md:18-20` permits “any order,” while lines 84-87 say “the type in the header is a convenience, not a requirement.” The latter also contradicts that design’s own earlier statement that `show` includes “the node’s type as the graph declares it.” Change `validation/P8/design.md`; owning node: `design`.

5. P8’s immutable baseline cannot coexist with the planned content edit. `chapters/04-library/SPRINT-04.md:52-55` says doctrine inclusion “changes prompt text, so the sprint 1 byte-equality snapshot is updated.” `validation/P8/design.md:75-80` requires every node’s output to equal “the pre-migration `Build`” from “an immutable baseline the coder cannot update.” Change `validation/P8/design.md`; owning node: `design`.

6. P6’s validator weakens chapter-ledger ownership. Decision 44 says: “The chapter ledger is written at plan time and edited only with a reason, by a human or the planner.” `validation/P6/design.md:40-44` accepts removal when “the stage across which it left is a codergen stage whose `response.md` names the chapter,” without requiring that stage to be the planner. Change `validation/P6/design.md`; owning node: `design`.

7. P10 repeats the same chapter-ledger authority drift. Decision 44 restricts chapter-ledger edits to “a human or the planner.” `validation/P10/design.md:80-84` accepts changes to a chapter’s `check`, `doc`, or `command` whenever they occur “across a stage whose `response.md` states the reason,” regardless of which node made them. Change `validation/P10/design.md`; owning node: `design`.

8. The holdout is recorded as private state of one run but consumed by another. `declaration.md:79` says the path is “recorded in the run’s private workflow state, rendered only into `verify`”; `planning-workflow.md:211-213` says `verify` reads “the run’s private workflow state … written there by the design lap.” But `validation/P10/design.md:69-74` requires the execution run to be “a different directory from the plan run’s” with “a different run id.” The plan run’s private state therefore cannot be the execution run’s private state. Change `declaration.md` and its mirrored `planning-workflow.md` rule; owning node: `brief`.

ROUTE: fail
