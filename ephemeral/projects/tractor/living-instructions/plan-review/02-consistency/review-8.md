1. `planning-workflow.md` names the research gate two ways. The graph says, “`intake ─▶ research ─▶ gate ─▶ brief ─▶ research ─▶ gate`,” while §3 says, “A tool node, `index_gate`, after the fan-in checks the index.” Change the graph to `index_gate`. Owning node: `brief`.

2. The research-output inventories disagree. `decisions.md` decision 52 requires “a routing index over it, and a pointer file,” but `planning-workflow.md` lists “`research/ plan.md, findings.md, INDEX.md, leaves`,” with no pointer file. Change `decisions.md` to name the intended canonical file or remove that requirement. Owning node: `brief`.

3. P8 drifts from “supervisor brief” to the materially broader “briefs.” `declaration.md` promises, “Every prompt body, doctrine page, supervisor brief, pass, and skeleton is a library file,” while `decisions.md` decision 59 says, “Prompt bodies, doctrine, briefs, passes, and skeletons are library files.” Since `planning-workflow.md` separately says the `brief` node “Writes `brief.md`,” the latter wording incorrectly includes a generated project artifact. Change decision 59 to “supervisor briefs.” Owning node: `brief`.

4. The validation-design directory has two placeholders. `declaration.md` names “`validation/<id>/design.md`,” while the `planning-workflow.md` layout names “`<promise>/ design.md`” and later “`validation/<promise>/`.” Change `planning-workflow.md` to use `<id>` consistently. Owning node: `design`.

5. The holdout directory-naming rule drifts. `decisions.md` decision 43 says it is “a directory named by a random token,” while `declaration.md` specifies “`$XDG_STATE_HOME/tractor/holdouts/<build>-<token>/`”; `planning-workflow.md` repeats the latter exact path. Change decision 43 to say the directory is named by the build plus a random token. Owning node: `design`.

6. Chapter 5 has contradictory replanning timing. `declaration.md` says later chapters “start with a backlog sketch and are re-planned each lap,” and `chapters/05-planner/CHAPTER.md` says “sprints in `sprints.md` (backlog, re-planned each lap).” But `chapters/05-planner/sprints.md` says, “Until chapter 6 adds `replan`, open items are edited only when the plan node runs (once per chapter …); decision 44’s per-lap replanning arrives with chapter 6.” Change `declaration.md` and the chapter-5 `CHAPTER.md` to state the chapter-5 bootstrap exception. Owning nodes: `brief` and `decompose`, respectively.

7. `SPRINT-03.md` both permits unused templates and rejects them. It says, “P8 does not promise that every skeleton is used, and sprint 4’s script checks its own skeletons are cited,” but its proof requires that “every template file’s sentinel appears in some node’s output.” The latter fails whenever a permitted unused skeleton exists. Change the sprint-3 proof requirement to cover only templates in a node’s include closure, leaving skeleton-use checking to sprint 4. Owning node: `decompose`.

8. The chapter-shape rule and its skeleton drift. `planning-workflow.md` defines “a chapter doc (pyramid index, vector, review posture, non-goals),” matching the concrete chapter 04–06 documents. But `chapters/04-library/content/templates/CHAPTER.md` additionally mandates “`## Design direction`,” “`## Architecture principles`,” “`## Sprint horizon`,” and “`## Sprint planning notes`.” Change the template to the canonical four-section shape, or explicitly expand the rule and every concrete chapter document. Owning node: `decompose`.

9. A failed plan-review pass cannot remain open under the stated loop rules. `planning-workflow.md` says fail routes to the owning node, “whose edge returns to the loop node, which re-selects the still-open pass,” while also saying the review-ledger item has no command. But `loop-node.md` §2 says, “An item with neither `command` nor `infer` passes when its lap returns.” Returning from the owning node therefore marks the failed pass done. Change `planning-workflow.md` and decision 48 so the owning node returns to `reviewer`, and only a passing reviewer returns to the loop. Owning node: `brief`.

ROUTE: fail
