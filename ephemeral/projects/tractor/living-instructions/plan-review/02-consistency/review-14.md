1. `workflow show` has two incompatible comparison oracles. `chapters/04-library/SPRINT-02.md:49-53` requires “for every node `show --raw` is byte-equal to a program … that calls `workflow.Build` directly.” But `chapters/04-library/SPRINT-03.md:42-45` requires “each node’s `show --raw` equals … standalone `workflow.Render` … (`Build` adds nothing to the rendering).” `SPRINT-01.md:54-56` confirms that “graph construction, path resolution, and validation stay in `Build`,” so standalone rendering cannot be the oracle for every node payload. Change `SPRINT-03.md` to use `Build`, or restrict the Render comparison to payloads for which equivalence is actually guaranteed. Owning node: `decompose`.

2. The render-test scope narrows from every template to only selected directories. `planning-workflow.md:287-290` says: “Tests render every template.” `chapters/04-library/sprints.md:23-24` likewise promises: “A test renders every template.” But `chapters/04-library/SPRINT-03.md:16-20` specifies only “every template under `prompts/`” plus standalone files under “`doctrine/` and `templates/`,” omitting the planned `supervisors/` and `passes/` templates. Change `SPRINT-03.md` so the named test exhaustively covers those directories too. Owning node: `decompose`.

3. `$goal` behavior is stated categorically and then contradicted by the documented comparison mode. `planning-workflow.md:294-296` says: “Frames and `$goal` are engine additions the raw output never reproduces …; `--stage --goal` expands `$goal` the engine’s way for the comparison only.” `chapters/04-library/SPRINT-02.md:39-44` specifies that expansion. But `chapters/04-library/SPRINT-05.md:6-11` orders the docs to “State plainly that `show` never reproduces frames or `$goal`,” without the raw-output qualification or `--stage --goal` exception. Change `SPRINT-05.md` to state the qualified rule. Owning node: `decompose`.

4. The template callables have two taxonomies. `declaration.md:61` names “the two value functions `quote` and `shell`, and the two composition actions `include` and `doctrine`”; `chapters/04-library/CHAPTER.md:34-36` repeats that distinction. `chapters.md:25-26` instead calls them collectively “the four library functions.” That erases the contract’s function/action distinction. Change `chapters.md` to use the declaration’s terminology. Owning node: `decompose`.

ROUTE: fail
