1. Chapter 5 builds research-library quality and fan-out behavior that P2 does not promise. `chapters/05-planner/sprints.md:18-21` requires “the research parallel node (at most five branches), fan-in, index update, findings file, and the index quality tool node” and proves “leaves and findings exist; the timeline shows the branches.” But `declaration.md:52` promises only one planted promise-changing finding, a tool-node halt, and that “the finding was asked, not applied,” explicitly disclaiming convergence on arbitrary seeds. Branch count, leaves, indexing, and index quality serve no numbered promise. Owning node: **brief**—add a research-quality promise if this is genuinely product scope.

2. Three of the four supervisors serve no promise. `chapters/05-planner/CHAPTER.md:25-27` plans “Four supervisors named by their question,” while `declaration.md:57` promises behavior only from `scope_cop`: “delivers at least one steer.” Nothing promises behavior from `research_auditor`, `slice_critic`, or `proof_skeptic`. Owning node: **brief**—either promise their necessary behavior or remove them from this plan.

3. `replan` is unpromised functionality. `chapters/06-execution/sprints.md:12-13` adds “`replan` in `medium` and `large`” with proof that it “edits only open items and never the chapter ledger.” P6 promises only completion and verify-before-mark ordering (`declaration.md:56`); P10 promises completion and seed acceptance examples (`declaration.md:60`). Neither requires a replan node or its edit restrictions. Owning node: **brief**.

4. The holdout writer/handoff has no covering promise. `chapters/05-planner/sprints.md:28-30` builds “`design` with both archetypes and the holdout writer,” and `chapters/06-execution/sprints.md:14-16` builds the handoff. Yet `declaration.md:62-64` says, “No promise here carries a holdout,” and `promises.md:24-25` excludes “a holdout in this project's proof.” P9 promises only that documentation explains where a holdout comes from, not that the writer or handoff works. Owning node: **brief** if holdout behavior is genuinely required.

5. Chapter 6 then directly plans the excluded holdout proof. `chapters/06-execution/sprints.md:16-17` requires: “Proof: a small package with one universal promise whose holdout sample the coder never sees.” That is precisely a holdout in this project’s proof, ruled out by `promises.md:24-25` and `declaration.md:62-64`. Owning node: **decompose**.

6. P1’s design introduces another withheld test input and greater generality than P1 requires. `validation/P1/design.md:12-15` says that at chapter verification “the `verify` node writes its own three features into the template, unseen by the coder.” P1 merely promises one seed naming three features (`declaration.md:51`), while the declaration says this project has no holdout. Calling the withheld features a scenario does not change their function as a holdout. Owning node: **design**.

7. Chapter 4 adds public `workflow show` modes beyond P8. `chapters/04-library/SPRINT-02.md:27-35` specifies both “`--node <id> --raw`” and “`--values` prints the template data struct.” P8 promises whole-graph materialized output and `--stage` diffing (`declaration.md:58`), not selective raw output or exposing template values. Those flags exist to make the proof script convenient, creating extra public surface and generality. Owning node: **decompose**.

8. P8’s orphan requirement is made stricter than the promise. P8 requires “every doctrine page is referenced by at least one prompt” (`declaration.md:58`). The sprint additionally requires that “every file under `prompts/` and `supervisors/` is in the include closure of some node” (`chapters/04-library/SPRINT-02.md:84-88`), and `validation/P8/design.md:74-76` repeats that rule. This rejects unused prompt or supervisor files even though P8 promises orphan-freedom only for doctrine pages. Owning node: **decompose**.

9. P6’s validator proves more than P6 states. P6’s declared verifier is a “timeline order check” (`declaration.md:56`), but `validation/P6/design.md:59-65` additionally fails unless every verifier “operate[d] the software (run it, not only read files)” and reported what it did. That operating-verifier behavior may be desirable, but it is a separate rigor guarantee absent from P6. Owning node: **design**.

10. P10’s validator adds an implementation-authorship condition. `validation/P10/design.md:73-75` requires “at least one `implement` stage's segment” to have written program source. P10 promises only that plan approves a valid package, execution reaches `COMPLETED`, and the resulting program satisfies the seed examples (`declaration.md:60`). A program scaffolded during planning and validated during execution could satisfy P10 as written; requiring execution-stage source authorship is stricter. Owning node: **design**.

11. P4’s fallback introduces a production test hook not promised by P4. `validation/P4/design.md:72-76` proposes `TRACTOR_TEST_TRIVIAL_FIRST=1`, “honored only when the seed asks for it,” if the model does not follow the scenario instruction. P4 promises a rejected design being re-entered and later passing (`declaration.md:54`); it does not promise or require a special environment-controlled prompt behavior. Owning node: **design**.

ROUTE: fail
