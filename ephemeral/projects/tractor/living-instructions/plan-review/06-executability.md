# Pass 6: executability

Question: can the plan be executed as written by the loop node? For
chapter 4's ledger: does each sprint fit one agent turn; does every
`command` run from the repository root as written (check paths, script
names, test names it invokes, and whether the scripts exist); does every
`doc` and `checklist` path resolve; do `chapters.md` and every
`sprints.md` parse as Markdown with YAML frontmatter in the format
`loop-node.md` §2 defines? For chapters 5 and 6: are the backlog
sketches sized so each entry is one sprint? Any broken path, unrunnable
command, or oversize sprint is a fail; owning node `decompose`.
