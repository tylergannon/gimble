# Pass 5: scope

Question: does anything in the plan exceed the promises? Read the
exclusions in `promises.md` and `declaration.md`, then every chapter
doc, sprint doc, and backlog sketch. Name any work that serves no
promise or that the exclusions rule out, and any place the plan builds
more rigor, security, or generality than a promise requires. Each is a
fail; owning node `decompose`, or `brief` if a promise should be added
to cover work that is genuinely needed.
