# Two unfinished text helpers

Finish the repository's `greeting` and `words` packages. Each package exposes
one stubbed function and has an intentionally failing contract test, but the
owner has not yet chosen the exact greeting format or what counts as a word.

Keep the packages independent and make the public behavior explicit before
implementation. The completed repository must pass all Go tests. Prefer the
smallest plan whose items can each be completed and validated in one coding
turn.
