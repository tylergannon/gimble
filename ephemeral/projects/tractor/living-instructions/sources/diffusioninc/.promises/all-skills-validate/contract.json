{
  "acceptance_gates": [
    {
      "description": "Both skill mirrors contain the same non-empty set of df-* skill bundles.",
      "id": "inventory"
    },
    {
      "description": "scripts/validate-skills.py exits zero for the full mirrored skill inventory.",
      "id": "validator"
    },
    {
      "description": "The validator and every bundled Python helper compile without syntax errors.",
      "id": "python-compile"
    }
  ],
  "badge": {
    "label": "Skills validate",
    "readme": "README.md"
  },
  "created_at": "2026-08-26T15:17:46Z",
  "economics": {
    "approval_required": false,
    "estimate_basis": "Recurring local deterministic validation and compact evidence recording; one-time Promise design is excluded.",
    "full_run_cost_band": "lt-10",
    "max_run_cost_usd": 9.99,
    "staging_plan": null
  },
  "evidence": {
    "mode": "internal"
  },
  "human_signoff_required": false,
  "id": "all-skills-validate",
  "invalidation": {
    "max_age_hours": null,
    "scope_change": true
  },
  "name": "All skills validate",
  "operation": {
    "authority": "Read-only validation and promise-state updates; skill remediation, commits, scheduling, and dirty review require explicit user authority.",
    "budgets": {
      "max_steps": 30,
      "max_tokens": null,
      "max_tool_calls": 50,
      "max_wall_seconds": 1800.0
    },
    "cadence": "manual",
    "plan_steps": [
      "Inventory skill bundles in both mirrors and verify the sets match.",
      "Run the repository validator and capture its exact result.",
      "Compile the validator and every bundled Python helper.",
      "Record one test result per skill bundle in each mirror for durable coverage.",
      "Record the validation report as passing evidence for every gate and finish every run with its truthful verdict; fulfillment requires every gate to pass."
    ],
    "runner": "Human-invoked local Codex task"
  },
  "schema_version": 2,
  "scope": {
    "exclude": [],
    "include": [
      ".claude/skills/**",
      ".agents/skills/**",
      "scripts/validate-skills.py"
    ]
  },
  "statement": "At the recorded repository subject fingerprint, every skill bundle under .claude/skills and .agents/skills passes scripts/validate-skills.py and every bundled Python helper compiles.",
  "tools": [
    {
      "path": "tools/run-validation.py",
      "purpose": "Run the full deterministic skill validation suite and write a compact JSON report."
    }
  ],
  "verifier": {
    "identity": "tools/run-validation.py",
    "path": "tools/run-validation.py",
    "type": "deterministic-tool"
  }
}
