1. Yes. The cheapest game is a detached watcher launched by an execution agent: agents receive `TRACTOR_RUN_DIR` ([Claude adapter](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/harness/claude/adapter.go:344)), and the timeline is an ordinary writable append-only file ([store.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/store.go:95)). The watcher can let the real run fail, then append the required success-stage and `PipelineCompleted` events and forge ledger/validation records. The observer would mirror these after no agent stage is live and misclassify them as engine-authored under its timing rule ([ledger.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/ledger.md:118)). Hard-coding the two small known programs makes the probes pass; no decoy node or engine/harness lie is needed, while the execution workflow never actually completed.

2. Yes. The engine-authorship check depends on process provenance that is not captured. The observer records only event arrival time and which stage appeared live, then assumes a turn “can only write while its own stage is live” ([ledger.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/ledger.md:118)); it records no writer PID, authenticated event source, or descendant-process lifetime. Consequently, an engine event and an append from a detached child are indistinguishable in precisely the evidence the command and infer judge read.

3. Yes. Decision 44 requires reasons for chapter-ledger edits, but for sprint ledgers it merely authorizes `replan` to edit open items ([decisions.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/decisions.md:200)). The validator additionally requires every renamed, changed, or removed open sprint item to have a response containing a grounded reason ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P10/design.md:101)). A conforming replan could split an open sprint after inspecting the repository, report only that it updated the ledger, and still execute both seeds successfully; P10 and decision 44 permit that, but this validator rejects it.

ROUTE: fail
