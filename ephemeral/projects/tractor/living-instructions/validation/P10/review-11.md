1. Yes. The cheapest game is to have the coder-owned execution graph iterate a shadow checklist containing the approved items’ same names and validator fields. Each shadow validator can mark the corresponding real package item `done: true`; the honest engine then emits the required event for the identically named shadow item. The program itself can be built during `plan`, which the design expressly allows. Thus the approved package was never actually executed, yet the name-based checks pass. This needs neither an engine lie nor a duplicate node, and the infer judge does not examine execution identity ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P10/design.md:67), [ledger.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/ledger.md:78)).

2. Yes. The assertion that “the approved package was what ran” depends on evidence that is not captured: `validation.json` records item name and command but no checklist path, while `LoopValidated` records only node, item name, result, and summary ([loop.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/loop.go:28), [loop.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/loop.go:90)). Consequently, an honest run of the approved ledger and a run of an identical shadow ledger can produce indistinguishable evidence under the validator’s stated comparisons.

3. No concrete false failure found. The immutable approved package, canonical MEDIUM/LARGE handoff, engine-validated completion, permitted replan changes, and exact seed examples follow the declaration’s package and execution contracts. The additional ledger checks enforce those seams rather than adding an outcome beyond P10.

ROUTE: fail
