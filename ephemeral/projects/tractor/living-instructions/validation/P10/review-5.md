1. Yes. The cheapest game is to short-circuit the coder-owned `medium`/`large` command path and print `COMPLETED`, while the planning run or a seed-specific hook writes the hard-coded program. The validator checks completion through `check.log` ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P10/design.md:52)), but does not require the execution run’s engine-authored `PipelineCompleted` event, which is the actual completion record ([runner.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/runner.go:342)). All examples could pass without `medium` or `large` executing the package, and no false engine events are needed.

2. Yes. The approval inference depends on evidence it does not receive. Its declared inputs include the question, answer, and approve turn, but not the actual package or review-outcome files ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P10/design.md:57)). The byte comparison only proves the package did not change after the answer; it cannot prove that a plausible summary and claimed pass outcomes in the question accurately describe that package. Thus the semantic approval check can accept approval of a fabricated description.

3. Yes. The inference explicitly fails when README build instructions do not work ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P10/design.md:63)), while P10 promises only the seed acceptance examples, as the design itself confirms under “Not proven” ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P10/design.md:71)). A correctly built program could satisfy every example but fail this validator because its README contains a stale build command.

ROUTE: fail
