1. Yes. The cheapest game is to make `plan` build seed-specific working programs while emitting valid approved packages, then make `medium`/`large` immediately route to success. The validator records only the final scratch repository and execution completion—not repository state before execution or meaningful execution-stage work—and its infer reads only approval/package material and final probes ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P10/design.md:44)). This passes while violating the contract that execution proves and runs the planned package ([planning-workflow.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/planning-workflow.md:169)).

2. Yes. `PipelineCompleted` proves only that the content-owned graph routed to success; the design checks no execution prompts, loop validations, or before/after repository evidence ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P10/design.md:55)). Consequently, the final program and probes cannot distinguish an honest execution from the planning-stage implementation above. Retaining the full run directories does not cure this because the validator never reads their semantic execution evidence.

3. Yes. The byte-identical-package requirement rejects a conforming run. The observer snapshots immediately before calling `tractor answer` ([ledger.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/ledger.md:127)), but the workflow’s interview directory is inside the package and the declared package layout includes its question and answer files ([planning-workflow.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/planning-workflow.md:192)). `tractor answer` then creates the sibling `.answer.md` file ([interview.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/cmd/tractor/interview.go:94)), so the final package necessarily differs from the pre-answer snapshot unless the comparison explicitly excludes `interview/`, which the design does not.

ROUTE: fail
