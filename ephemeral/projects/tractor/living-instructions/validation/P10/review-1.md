1. Yes. The cheapest game is to prepopulate each scratch repository with working software and self-passing tests, then let `plan` produce—or replace its output with—a structurally valid package whose checklist commands are no-ops. The design retains only the final repository and package, with no initial snapshot, package digest, or proof that execution created the software (design.md:20-24). `validate-plan` checks artifact shape and sizing, not semantic coverage (workflow/artifacts.go:29-66), so both runs can reach `COMPLETED` while the planning/execution capability is false.

2. Yes. “The scratch repository’s own tests pass” is trivially satisfiable because the coding agent also authors those tests. More importantly, the claimed fixed-seed provenance is not captured: design.md:7-14 says the seeds were already committed before chapter 5, but neither seed exists in this checkout and `git log --all --` has no history for them. No hash or baseline commit is specified, so the seeds can be created or weakened later. The infer check names only the mutable README and seed and specifies no fixed functional probes or expected outputs (design.md:34-36).

3. Yes. The promise permits the resulting execution to be `medium` or `large`, but the design requires greeter specifically to be MEDIUM and ledger specifically to be LARGE (design.md:10-14, 28-31). Because size follows the chosen decomposition—MEDIUM means multiple flat items while LARGE means multiple chapter items (workflow/artifacts.go:69-85)—a correct planner could produce a valid LARGE greeter or MEDIUM ledger and run it to `COMPLETED`, satisfying the stated promise but failing this validator.

ROUTE: fail
