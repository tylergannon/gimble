1. Yes. The cheapest game is to run one `implement` turn and one `verify` turn before any chapter is selected, then route through only `chapters` and empty `sprints` loops. The same old `verify → chapters` completion remains the “nearest preceding” non-loop stage for every later chapter mark, because the validator explicitly ignores both loop nodes and never requires a verify after that chapter’s `LoopItemSelected` ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P6/design.md:31)). All engine events remain truthful, but no verify turn belongs to any chapter routing pass.

2. Yes. The ledger claims snapshots represent exact stage boundaries ([ledger.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/ledger.md:109)), but the observer merely tails the timeline asynchronously. After writing `StageStarted`, the engine immediately executes the handler ([runner.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/runner.go:474)); a loop can validate and mark `done` before the observer copies anything ([loop.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/loop.go:83)). Thus the flip and no-other-stage-mutation checks depend on boundary state the design does not reliably capture.

3. Yes. Requiring the entire chapter ledger to remain byte-identical across every `plan`, `implement`, `replan`, and `verify` stage is stronger than P6, which constrains only ordering of `done: true` after verification ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P6/design.md:41)). A planner could make a reasoned non-`done` correction and still satisfy P6; the project expressly permits planner edits to the chapter ledger with a reason ([decisions.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/decisions.md:197)).

ROUTE: fail
