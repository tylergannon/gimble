1. Yes. The cheapest game is to let the first chapter complete verification, then have that `verify` turn delete all remaining chapter items. The validator checks only chapters still present in the final ledger and explicitly allows other chapter-ledger edits ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P6/design.md:31)). Although a LARGE plan initially has multiple chapters ([artifacts.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/workflow/artifacts.go:81)), the loop treats vanished items as gone and can complete the shortened ledger ([loop.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/loop.go:74)). Thus the run passes while original chapters were never marked or verified, without falsifying engine events.

2. Yes. The “no early `done`” check depends on snapshots representing the exact `StageCompleted` instant ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P6/design.md:41)), which the observer does not capture atomically. After emitting `verify`’s `StageCompleted`, the runner immediately enters the loop ([runner.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/runner.go:514)); that loop emits `LoopValidated` and marks the item before an asynchronous observer may finish copying ([loop.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/loop.go:90)). The ledger itself says snapshots must not time loop marking ([ledger.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/ledger.md:93)), contradicting this check.

3. Yes. P6 requires completion and verify-before-done ordering, but the design additionally requires different harnesses for `verify` and `implement` and restricts every `replan` to changing only the current `sprints.md` ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P6/design.md:38)). An implementation using the same harness—or a replan that legitimately updates another planning artifact—could satisfy P6 exactly and still fail this validator.

ROUTE: fail


