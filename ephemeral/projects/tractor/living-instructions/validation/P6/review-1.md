1. Yes. The cheapest game is for the `plan` stage to write `done: true` before `verify`, then continue through `verify`. The validator forbids chapter-ledger edits only by `replan` and `implement` ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P6/design.md:25)). When the chapters loop returns, it honors an already-done item without validation or a `LoopValidated` event ([loop.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/loop.go:74)). Thus the validator’s universal check is vacuous for that chapter even though its marking preceded `verify`.

2. Yes. `PipelineCompleted` does not capture `next`; it contains only `duration` ([runner.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/runner.go:342), [spec.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/docs/spec.md:2496)). Also, stage evidence does not automatically capture filesystem diffs or attribute ledger edits: codergen records prompt/response, while stage completion records only the outcome ([codergen.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/codergen.go:57), [store.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/store.go:149)). Therefore the replan-diff and mutation-ownership checks depend on evidence the design does not actually capture.

3. Yes. P6 requires only a preceding `verify` turn routing pass; the design additionally requires every verifier to execute the built software and capture output for every validation-story step ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P6/design.md:28)). A correct implementation could verify a documentation-only chapter from static artifacts or previously captured test evidence, route pass, and then mark it done, yet fail this extra command-execution requirement.

ROUTE: fail
