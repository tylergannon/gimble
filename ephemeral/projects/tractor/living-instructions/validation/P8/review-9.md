1. Yes. Cheapest game: place an instruction in a non-rendering template branch, such as `<<if not .Project>>…<<end>>`, and emit that same instruction from a Go string. The residue check strips the actions and therefore finds the instruction in the closure, while per-file sentinels still render normally. `show` equals `Build`, snapshots remain byte-identical, and no `infer` inspects authorship ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:61)). The promise is nevertheless false because Go supplies prompt text, contrary to [decision 53](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/decisions.md:250).

2. Yes. The claimed provenance check captures textual membership and file influence, not the origin of each rendered byte. Sentinels prove that a library file affects output, while residue membership proves only that equivalent text exists somewhere in its closure ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:67)); neither distinguishes honest template emission from the Go-emitted substitution above. Thus the assertion that Go-supplied text fails depends on authorship evidence the design never captures.

3. Yes. A correct prompt may include the same library fragment twice—for example, two `include "shared"` actions—while all content remains file-backed and Go supplies only data. P8 and its template seam impose no single-use restriction ([declaration.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/declaration.md:58)), but the validator counts each closure file once and rejects when rendered residue lines exceed closure residue lines ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:80)). That is a concrete false failure.

ROUTE: fail
