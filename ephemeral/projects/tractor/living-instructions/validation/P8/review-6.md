1. Yes. The cheapest game is to keep the real pass or skeleton text in Go-generated checklist data—which the engine legitimately injects into the runtime frame—while placing unused, valid stubs under `passes/` or `templates/`. The sentinel check never requires every pass file to appear, does not mutate templates, and checks only node output ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:55)); the real-stage check then strips the frame containing the illicit text ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:69)). This violates P8 without any engine lie or decoy node.

2. Yes. The “data only” check depends on provenance it does not capture: it examines rendered `show --raw` lines, while Go-generated pass/checklist content delivered through frames is absent from that output and deliberately removed from the recorded-stage comparison ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:60)). Rendering every library file proves syntax, not that the runtime content came from those files. Thus an honest recording cannot distinguish a file-backed pass from a Go-backed pass carried in the stripped frame.

3. Yes. A correct implementation may compose a node’s top-level prompt with a library-backed pass or supervisor include—the planned template machinery explicitly permits `include` composition ([SPRINT-01.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/SPRINT-01.md:45)). The validator nevertheless requires the output to contain the header-named file’s sentinel and no sentinel from any other prompt, supervisor, or pass file ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:56)). That rejects a wholly library-backed, contract-permitted multi-file prompt.

ROUTE: fail
