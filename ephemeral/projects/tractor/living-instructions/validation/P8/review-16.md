1. Yes. The cheapest game is a run-only prompt append in `cmd/tractor`: mutate a medium-workflow node after `Build` and before execution—the current handoff occurs between [`Build`](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/cmd/tractor/workflow.go:124>) and [`run`](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/cmd/tractor/workflow.go:139>). `show` would still equal unmodified `Build`; the real-stage probes cover plan and one large node, not medium ([design.md:129](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:129>)); and `infer` reads prompt-producing Go only under `workflow/`, not `cmd/tractor/` ([design.md:145](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:145>)). This cheaply violates file-only prompt authorship without engine or harness dishonesty and survives the specified judge evidence.

2. Yes. The “text from the closure only” check is tautological for the header file: the closure always contains that file ([lib.sh:35](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/prove/lib.sh:35>)), and the check renders it through `workflow.Render` before comparing it with `show --raw` ([orphan-walk-and-render.sh:55](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/prove/orphan-walk-and-render.sh:55>)). Because the design already requires `show --raw == Build` and `Build == Render(header)`, every output line necessarily occurs in that rendering. Contrary to the claim at [design.md:108](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:108>), Go text added inside `Render` would pass this check; only the separate code-reading judge could detect it.

3. Yes. A permitted template could conditionally reference doctrine—for example, include `threat-model` only when `.Project` has a particular valid value. That satisfies the promise because the page is a library file referenced by a prompt, and the design expressly allows conditional includes to be absent for its chosen parameters ([design.md:93](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:93>)). Nevertheless, its doctrine sentinel rule requires every prompt naming the selected page to print it ([design.md:95](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:95>)), so a correct conditional reference that does not fire for the fixed test parameters fails.

ROUTE: fail
