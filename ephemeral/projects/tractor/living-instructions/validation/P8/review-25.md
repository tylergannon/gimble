1. Yes. The cheapest game is a new prompt-bearing node whose library file renders one token, such as `Review`, while `show` omits its source file. The proof classifies a payload as a prompt only when the pre-mutation dumper output contains whitespace ([orphan-walk-and-render.sh:53](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/prove/orphan-walk-and-render.sh:53>)); otherwise it skips the source and sentinel checks. Node census and payload equality still pass, and the infer question evaluates only probes the logs name ([design.md:184](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:184>)), so the skipped node produces no failing evidence.

2. Yes. The source-attribution check depends on whitespace, which does not distinguish prompts from commands, checklist paths, or other payloads. Although the design requires every prompt-bearing node to name its library file ([design.md:110](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:110>)), the dumper records only payload text and the script consults the node type only after the whitespace test. Consequently, its captured evidence cannot establish that every prompt-bearing node underwent the attribution check.

3. Yes. The mutation may replace an ASCII prompt byte with an invalid UTF-8 byte, yet the design requires exactly exit 1 and a line-oriented diff ([design.md:103](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:103>)). A correct implementation may diff every real, valid-text Tractor stage but reject malformed UTF-8 with exit 2; the promise requires real-stage comparison, not binary-corruption handling, so that permitted implementation can fail nondeterministically.

ROUTE: fail
