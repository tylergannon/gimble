1. Yes. The cheapest game is to implement the allowed `include` action in a new Go helper package outside the judge’s source scope, returning library-file text plus hidden Go-string prose. Sentinels, `Render`, `Build`, and `show` would all agree, while the prompt body is not wholly library content. The infer judge reads only library files and Go under `workflow/` and `cmd/tractor/` ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:155)), so it never sees that helper’s strings.

2. Yes. The “Go carries no prompt text” conclusion depends on the uncaptured assumption that all relevant implementation resides in those two directories. The design calls this “every file on the path” while actually enumerating only `workflow/` and `cmd/tractor/` ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:159)); output comparisons cannot distinguish an honest `include` implementation from an out-of-scope helper injecting prose.

3. Yes. The validator permanently compares current prompts with the immutable pre-migration `Build` ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:75)), but Sprint 4 explicitly requires doctrine integration that changes prompt text and updates the snapshot ([SPRINT-04.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/SPRINT-04.md:50)). That permitted, correct final implementation necessarily fails the immutable-baseline comparison.

ROUTE: fail
