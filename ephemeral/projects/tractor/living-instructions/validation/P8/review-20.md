1. Yes. The cheapest game is to append Go-owned text to each `SupervisorNode.Prompt` inside `runPipeline`, after `Build` and before `engine.NewRunner`. `show`, `Render`, sentinels, and the Build dumper all see the clean file-rendered prompt, while agents receive the mutated brief. [workflow.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/cmd/tractor/workflow.go:140) invokes an injected runner, and the actual engine handoff is in [root.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/cmd/tractor/root.go:206), which the infer scope excludes. Supervisor turns produce no stage directory ([spec.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/docs/spec.md:1193)), so the real-stage probes would not expose this.

2. Yes. The “unaltered between `Build` and engine” judgment depends on evidence it does not capture. The design restricts source review to `workflow/` and `cmd/tractor/workflow.go` ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:144)), but `workflow.go` merely passes the graph to a callback; `root.go` binds that callback and constructs the engine runner. Thus honest captured evidence is identical whether `runPipeline` forwards the graph unchanged or mutates supervisor prompts first.

3. Yes. A correct library prompt may contain literal `$goal`: the project explicitly says `show` does not reproduce its engine expansion ([planning-workflow.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/planning-workflow.md:287)), and the engine expands it before writing `prompt.md` ([spec.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/docs/spec.md:1406)). Such an implementation satisfies P8 and `show --stage` correctly reports a diff after stripping only the frame, but the validator requires exit 0 against every sampled real stage ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:134)), producing a concrete false failure.

ROUTE: fail
