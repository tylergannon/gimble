1. Yes. The cheapest game is to rename the four prompt functions, split the searched planner sentence across literals, and leave the prompt bodies in Go; dummy library files plus incidental `go:embed` and `text/template` usage satisfy the purely lexical checks in [prompts-are-library-files.sh](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/prove/prompts-are-library-files.sh:6). `show` can legitimately call that still-Go-backed `Build`, so both synthetic and real-stage comparisons pass while P8’s content-location promise remains false.

2. Yes. The `go:embed` and `text/template` checks establish presence, not that `Build` obtains prompt content from them. More importantly, the validator merely requires PASS lines from coder-owned tests in [show-and-orphan-walk.sh](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/prove/show-and-orphan-walk.sh:71); it does not independently inject an orphan or verify that the synthetic-orphan case reaches the production walker. Its snapshot loop also ranges over existing snapshot files rather than enumerating Build’s nodes, so omitted nodes are invisible ([lines 32–42](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/prove/show-and-orphan-walk.sh:32)).

3. Yes. P8 requires every doctrine page—but not every skeleton—to be referenced ([declaration.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/declaration.md:58)). The design instead makes every file under both `doctrine/` and `templates/` subject to the orphan rule ([SPRINT-02.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/SPRINT-02.md:43)). Therefore a valid library-resident but currently unused skeleton satisfies the stated promise yet fails this validator.

ROUTE: fail
