1. Yes. The cheapest game is to leave a real doctrine page uncited while implementing `TestLibraryNoOrphans` to fail only when it sees the validator’s fixed `doctrine/zz-uncited.md` probe. The validator merely requires that exact injected name appear in the failure, then accepts the unmodified tree’s passing test; no independent walk or infer check observes other orphans ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:90), [show-and-orphan-walk.sh](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/prove/show-and-orphan-walk.sh:273)). That special case is cheaper than implementing the promised exhaustive orphan walk and needs no engine lie or decoy node.

2. Yes. The orphan mutation check depends on uncaptured test semantics: failure naming `zz-uncited.md` cannot distinguish a genuine tree walk from the filename trigger above. The rendering probe has the same defect—a test can recognize the fixed `broken-action-never-closed` mutation while accepting other malformed templates ([show-and-orphan-walk.sh](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/prove/show-and-orphan-walk.sh:278)). The design explicitly has no infer stage that could examine those implementations ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:107)).

3. Yes. The design requires every statically discovered include in a prompt’s transitive closure to render its sentinel for the validator’s chosen parameters ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:71)). A correct template may conditionally include a library fragment—for example, `<<if eq .Project "special">><<include "special">><<end>>`—and legitimately omit it when the validator supplies `demo`. The declared seam permits `text/template`, parameter-derived fields, and `include` actions without requiring every include to execute for every parameter set ([declaration.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/declaration.md:77)); such an implementation satisfies P8 but fails this sentinel check.

ROUTE: fail
