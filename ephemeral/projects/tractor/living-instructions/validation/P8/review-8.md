1. Yes. The cheapest game is to keep most prompt text assembled in Go, render only a small fragment from each cited file, and place copies of the remaining hard-coded lines inside template comments or unrelated library files. The validator compares each rendered line against a global unordered residue set—not the cited file’s rendered closure—so this passes; moving the single grepped planner sentence also defeats the Go-string tripwire. Since both `show` and the real run consume the same false `Build`, their equality checks still pass. Evidence: [design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:56) and [show-and-orphan-walk.sh](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/prove/show-and-orphan-walk.sh:152).

2. Yes. The sentinel checks establish that each named file contributes some bytes, but do not establish that the rest of the prompt or pass came from that file. The residue check loses source, order, and multiplicity by pooling all library lines. Moreover, the actual script obtains its “known” data values from the coder-owned `workflow show --values`, despite the design claiming independent computation; a dishonest implementation can report Go-supplied prose as data and have it removed. Evidence: [design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:65) and [show-and-orphan-walk.sh](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/prove/show-and-orphan-walk.sh:136).

3. Yes. A correct template using the expressly required `quote` function can fail the residue comparison. For `- Project: <<quote .Project>>`, rendering `demo` produces `- Project: "demo"`; removing the raw data value leaves `- Project: ""`, while removing the template action from the library line leaves `- Project: `. The same problem affects `shell` quoting. Both transformations are explicitly required by the project’s template contract, so rejecting them is a concrete false failure. Evidence: [SPRINT-01.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/SPRINT-01.md:39) and [design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:76).

ROUTE: fail
