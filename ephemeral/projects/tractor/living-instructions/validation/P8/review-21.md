1. Yes. The cheapest game is to omit synthesized structured-parallel branch nodes from headed `show` while retaining `--node` support. The validator enumerates only YAML lines matching exact top-level indentation ([lib.sh](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/prove/lib.sh:28)), then calls the Build dumper only for those IDs ([show-equals-build.sh](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/prove/show-equals-build.sh:10)). But parsing structured branches appends synthesized codergen nodes to `Graph.Nodes` ([parse.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/graph/parse.go:82)). Put such a node outside the narrowly sampled medium/large `implement` stages, and every check passes while headed `show` omits a real graph node. The infer evidence excludes `graph/parse.go` and records only a success summary, not the actual Build node inventory, so the judge cannot reliably recover the omission.

2. Yes. The claimed “every node” comparison depends on an actual Build-node census that is never captured. Although the design says Build output is collected for every payload-bearing node ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:42)), the script derives its universe from indentation-sensitive `awk`, not `g.Nodes`; the dumper’s real `g.Nodes` loop is queried only by those preselected IDs ([lib.sh](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/prove/lib.sh:86)). Consequently the evidence cannot distinguish complete output from omission of synthesized nodes.

3. Yes. A correct fan-in implementation would fail the real-stage zero-diff requirement. The design expects exit 0 for every prompt-bearing plan stage after stripping only frames and expanding `$goal` ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P8/design.md:134)), but the existing engine appends rendered branch results to a fan-in’s Build-materialized prompt before writing `prompt.md` ([fan_in.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/fan_in.go:64)). Reporting that runtime-added evidence as a diff is permitted—and expected—by a promise that requires only the frame to be stripped and expressly does not require `show` to reproduce runtime additions; demanding no diff rejects that correct implementation.

ROUTE: fail
