1. Yes. The validator’s entire random space is disclosed: three tokens chosen from a fixed twelve-feature pool ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P1/design.md:7)). A coder could special-case those tokens, ask the fixed promise “accepts stdin,” and record its scripted rejection, while doing no elicitation for other three-feature seeds. This is cheaper than implementing general anticipation, survives the infer review, and requires no engine or harness deception.

2. No substantive defect. The answer file and `answers.log` are observer-produced and therefore partly redundant, but together with the matching `QuestionAsked` path and final semantic infer they identify the declined candidate. The unbound “any bracketing call qualifies” check is loose, yet `QuestionAsked` is itself emitted when `tractor ask` moves the question and blocks ([spec.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/docs/spec.md:517)), so that redundancy does not leave the promised evidence uncaptured.

3. Yes. The command rejects any pre-answer `brief.md` containing any exclusion entry ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P1/design.md:49)), although P1 requires only that the newly declined promise ultimately appear there. A permitted planner could first ask a non-`Promise:` scope question, record an agreed “no GUI” exclusion, then ask and decline the novel stdin promise and append “no stdin”; P1 is satisfied, but this validator fails.

ROUTE: fail
