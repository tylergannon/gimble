1. Yes. After a genuine passing `LoopValidated`, the cheapest game is a later coder-owned stage reopening the ledger item before completion. The validator checks the passing event and `validation.json`, but never the final ledger state ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P4/design.md:30)); P4 explicitly requires the item to end `done: true` ([declaration.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/declaration.md:54)). This needs no false engine events.

2. Yes. Although an end snapshot is captured, the validator uses it only to compare `design.md`; neither the command nor infer checks the final ledger ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P4/design.md:23)). Additionally, agreement between `response.md` front matter and `StageCompleted.next` is mechanically duplicative: both derive from the same `Outcome.Next` ([codergen.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/codergen.go:276), [runner.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/runner.go:505)).

3. Yes. The infer check requires D2 to actually take in and respond to the notes, while the promise requires only that the notes be available. It also requires the second review to pass immediately; a correct run with another rejection and a later third-lap pass would satisfy “later routes to pass” but fail this validator’s fixed two-review sequence ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P4/design.md:30)).

ROUTE: fail
