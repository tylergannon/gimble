1. Yes. The cheapest game is to make `workflow show --node planner --raw` return a convincing but incomplete prefix of the real planner prompt. The command accepts any non-empty substring of `prompt.md`, while the infer judge does not read that `prompt.md` ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P9/design.md:57)). Thus docs and help can agree and the judge can pass even though `show` does not print exactly what `Build` materialized, contrary to the project contract ([planning-workflow.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/planning-workflow.md:255)).

2. Yes. The non-empty-substring comparison is trivially satisfiable without proving equality. Separately, the design says an observer attached to the reader run answers later nested-plan questions ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P9/design.md:15)), but the shared observer tails only the single run directory it receives ([ledger.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/ledger.md:120)); it cannot see the nested plan’s separate timeline, so completion depends on question handling the stated tooling does not provide.

3. No. The source isolation, singular completed plan run, real `ask`/`answer` exchange, planner-specific `show`, and provenance report are direct scenario instances of P9. The design also expressly concedes documentation completeness and the actual holdout handoff, so those cannot create a false failure ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P9/design.md:75)).

ROUTE: fail
