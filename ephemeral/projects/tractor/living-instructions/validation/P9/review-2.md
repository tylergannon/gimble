1. Yes. The reader is directly told the command names and is only required to execute `answer` and `workflow show`; no reader `ask` call is checked ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P9/design.md:10)). The observed `QuestionAsked` can instead come from the inner planner, whose runtime prompt supplies the exact `ask` command ([workflow.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/workflow/workflow.go:250)). Cheapest game: omit usable `ask` guidance from the promised docs while leaving the planner’s private instructions working; the validator still passes.

2. Yes. The holdout check merely requires `response.md` to name a predetermined root ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P9/design.md:40)); it captures neither the private workflow state nor the `verify` prompt where the actual holdout handoff occurs. Likewise, `QuestionAsked` followed by `StageCompleted` is assumed to mean execution continued after the answer, but the design never orders the separate reader `answer` tool call against those plan-run events.

3. Yes. A correct agent could print the planner prompt using the specified valid `--node <id> --raw` form, which intentionally has no header ([SPRINT-02.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/SPRINT-02.md:22)). It would satisfy the promise but fail the validator’s unconditional requirement that the result contain a `== ` header ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P9/design.md:38)).

ROUTE: fail
