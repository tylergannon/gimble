1. Yes. The cheapest game is to document `cat <plan-run>/stages/.../prompt.md`; the reader can print that file without ever running `workflow show`. The validator only requires a tool result contained in `prompt.md`, not a `workflow show` tool call, and `infer` checks used commands for documentation/help consistency rather than requiring the promised command ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P9/design.md:39)).

2. Yes. “Any later event” after `tractor answer` is merely temporal and may be an unrelated supervisor event; the design does not capture the child run’s paired `tractor ask` result or another event causally tied to that blocked call ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P9/design.md:43)). It also checks only where the holdout lives, not that the reader knows it comes from the validation-design lap, which is part of the promised understanding ([planning-workflow.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/planning-workflow.md:105)).

3. Yes. The validator requires exactly one plan-run directory inside the scratch directory ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P9/design.md:41)), but `--logs` is optional and a correct invocation may place logs under the XDG or home state root ([spec.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/docs/spec.md:570)). Unless the scenario explicitly relocates that state root, a correct agent can fail.

ROUTE: fail
