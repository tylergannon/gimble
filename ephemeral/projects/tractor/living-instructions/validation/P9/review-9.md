1. Yes. The cheapest game is to special-case `workflow show` under the scenario’s distinctive scratch/XDG setup: emit deterministic canned text for `--raw` and make `--stage` return success. The validator compares the reader’s output only with another invocation of the same executable and trusts that executable’s exit status ([design.md:64](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P9/design.md:64>)); the infer judge does not receive the actual planner `prompt.md` ([design.md:74](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P9/design.md:74>)). Thus the agent can print something other than the prompt actually received while every stated check passes, without an engine or harness lie.

2. Yes. Both show checks are self-referential: two calls to the same defective `show --raw` establish only consistency, while `show --stage` is allowed to grade itself. Although the real planner prompt is listed as evidence, the validator never independently compares it with the show output. The project’s chapter-4 proof demonstrates the missing pattern: compare against an independent `workflow.Build` dumper and require failure after a one-byte mutation ([show-and-orphan-walk.sh:94](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/prove/show-and-orphan-walk.sh:94>), [show-and-orphan-walk.sh:110](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/chapters/04-library/prove/show-and-orphan-walk.sh:110>)).

3. Yes. The infer rule requires every command, flag, and path used for the tasks to come from the promised sources ([design.md:74](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P9/design.md:74>)), while P9 promises correct use only of `workflow run plan`, `workflow show`, and `ask` ([declaration.md:59](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/declaration.md:59>)). A correct agent must create a seed and question in the initially empty repository and could legitimately use an undocumented helper such as `printf`, a heredoc, or an editor. Literal enforcement would reject that correct implementation for a requirement the promise and seams do not impose.

ROUTE: fail
