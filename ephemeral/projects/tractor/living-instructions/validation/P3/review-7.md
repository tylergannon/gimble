1. Yes. The cheapest game is to have the first `design` turn write all designs, perform one genuine `review` turn for P1, then use a conditional tool node to return directly to the loop for every remaining item. Because items without validators pass whenever a lap returns, the engine honestly marks each item ([engine/loop.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/loop.go:83)). The validator merely reuses the last completed codergen node as R for every later validation ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P3/design.md:41)), while `infer` examines only completed R turns ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P3/design.md:54)). Thus P2–P10 can be marked without their required review turns.

2. Yes. After one R stage completes, the “last completed codergen stage before the loop” check becomes a reusable stale witness; it never requires an R `StageCompleted` after the current item’s `LoopItemSelected`. The recorded timeline contains enough evidence to make that correlation, but the validator does not use it. Its semantic check repeats the same weakness by quantifying “for each R turn,” not “for each promise/item” ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P3/design.md:54)).

3. No. I found no concrete permitted implementation that fulfills the declared validation loop yet fails these checks. The apparently stronger requirements—one item per promise, a `design → review → loop` lap, reviewer/provider separation, and engine-owned marking—are all required by the project’s governing workflow ([planning-workflow.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/planning-workflow.md:105)) and decision 47 ([decisions.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/decisions.md:216)).

ROUTE: fail
