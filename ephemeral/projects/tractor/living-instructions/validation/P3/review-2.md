1. Yes. The cheapest game is to have `decompose` create the inspected `validation/ledger.md` already marked done, while configuring the validation loop to use a decoy checklist containing the same IDs. Genuine reviews and `LoopValidated` events then occur against the decoy. The validator compares only item names, while the event records `node`, `item`, and `passed`—not the checklist path ([design.md](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P3/design.md:26>), [loop.go](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/loop.go:90>)). This requires no false engine events.

2. Yes. The claim that each `LoopValidated` proves the engine marked the inspected ledger depends on checklist identity that is not captured. The engine resolves an arbitrary checklist path and marks that path ([loop.go](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/loop.go:62>)), but the emitted event omits it. Therefore the validator cannot correlate its final ledger with the ledger actually validated and mutated by the engine.

3. Yes. The validator requires the review key to carry the no-thread marker, proving fresh context ([design.md](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P3/design.md:31>)). P3 requires only a review on a different provider ([declaration.md](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/declaration.md:53>)); a correct implementation using a persistent review session on that different provider would fail.

ROUTE: fail
