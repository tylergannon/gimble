1. Yes. The cheapest game is a halt command that evaluates the real predicate during the validator’s standalone probes but returns success when it detects a live Tractor-run environment. The run can therefore reach `decompose` while a finding remains open, even though decision 46 requires both findings and plan entries to be clear ([decisions.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/decisions.md:210)). Research can still read the planted leaf, ask the expected question, and leave promises unchanged. This needs only a small conditional in library content; the engine truthfully records the resulting success route.

2. Yes. The three-state predicate test is dependent on uncaptured equivalence between the standalone execution environment and the actual tool-node invocation. The engine sets `TRACTOR_RUN_DIR` during runs ([runner.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/runner.go:245)), while the design only says the command is rerun “in the finished package” ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P2/design.md:25)). Neither `tool.log` nor `StageCompleted(next)` establishes that the command applied the tested predicate during the run, and the validator never checks that findings and plan entries were clear at the final halt.

3. No. The three predicate states, tool-node identity, research non-authorship of the brief, and final route all follow decision 46 and the documented node contract ([planning-workflow.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/planning-workflow.md:69)). Post-answer application is allowed, arbitrary-seed convergence is excluded, and unmatched paraphrases are inconclusive rather than failures. I found no permitted correct implementation that these checks necessarily reject.

ROUTE: fail
