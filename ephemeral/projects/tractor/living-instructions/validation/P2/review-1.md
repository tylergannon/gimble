1. Yes. The cheapest game is for `brief` to change CSV to JSON and commit before asking, then emit a vacuous question that merely mentions the planted filename. The validator checks authorship and question existence, but not that the answer preceded the promise change or that the question actually asks whether to apply the finding ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P2/design.md:28)).

2. Yes. The “not preceded by a `max_visits` exhaustion event” check is trivially true because no such timeline event exists; exhaustion only removes offered successors ([spec.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/docs/spec.md:802)), while the defined loop events contain no exhaustion event ([spec.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/docs/spec.md:2516)). Also, `StageCompleted` records only the node name/index/next, not its type, so the captured timeline does not establish that `halt` was a tool node ([runner.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/runner.go:514)).

3. Yes. A correct implementation could cite the finding by title, quotation, or source identifier without including its literal filename, failing the filename check despite satisfying the promise. The required per-lap commit and self-declared stage-index trailer are also extra constraints absent from the promise ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P2/design.md:31)).

ROUTE: fail
