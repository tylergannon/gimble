1. Yes. The cheapest game is to give the coder-owned fan-in a gutted prompt that writes the expected finding without reading the planted leaf, then make `halt` alternate routes using a visit counter. The validator captures when the finding appeared and which route was taken, but its `infer` never reads the research prompt/response or halt command ([design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P2/design.md:32)). This directly violates the shared rule that coder-owned names and edges prove nothing ([ledger.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/ledger.md:76)).

2. Yes. “The planted finding sent it back” is not captured: `StageCompleted.next` records only the selected successor, while the tool outcome records only its exit-derived route ([tool.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/tool.go:30)). Nothing establishes that the exit depended on `findings.md`. Also, requiring a “success `outcome.json`” alongside `StageCompleted` is redundant because the engine writes that file before emitting every successful `StageCompleted` event ([runner.go](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/runner.go:505)).

3. Yes. Requiring at least one `halt → brief` route is stricter than P2’s stated verifier, which requires only the eventual `halt → decompose` and one citing question. A correct run could have initial research produce the finding, `brief` ask it, subsequent research resolve it, and the first halt route directly to `decompose`; that satisfies P2 but fails the added requirement in [design.md](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P2/design.md:33).

ROUTE: fail
