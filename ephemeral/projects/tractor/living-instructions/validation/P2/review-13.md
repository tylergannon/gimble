1. Yes. The cheapest game is to let research derive the finding, have `index_gate` clear it so `halt` routes to `decompose`, then have `decompose` restore and ask about the finding afterward. The validator never requires the matched `QuestionAsked` to precede the final halt or originate in `brief` ([design.md:55](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P2/design.md:55)); its infer inputs omit the asking stage and timeline. This would satisfy every listed check while violating decision 46’s required sequence that `brief` asks about findings before the tool halts the loop ([decisions.md:211](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/decisions.md:211)).

2. Yes. The claim that the `StageStarted` observer copy is “the state the predicate saw” depends on an instant the design does not capture. The engine appends `StageStarted` and immediately invokes the handler ([runner.go:474](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/runner.go:474)); the ledger itself says that snapshot may include writes made afterward ([ledger.md:99](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/ledger.md:99)). Re-running the command after `COMPLETED` also cannot prove its active-run behavior because the run directory’s temporal state differs, despite the same environment variables.

3. No. The three-state halt predicate, success/error routes, and prohibition on research changing the brief follow decision 46 and the documented node contracts ([planning-workflow.md:69](/Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/planning-workflow.md:69)). The design expressly permits applying an accepted change after the answer, so it does not reject that concrete correct behavior.

ROUTE: fail
