1. Yes. The cheapest game is to run `tractor ask` in the background, wait until the observer has copied the `QuestionAsked` snapshot, change CSV to JSON before the answer arrives, then wait for `ask` to finish. The evidence still shows CSV at the question and JSON at stage completion, while research files remain unchanged. Because the design captures no state at answer time, it would pass even though the finding was applied without authorization. [`design.md`](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P2/design.md:39>) [`interview.go`](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/cmd/tractor/interview.go:43>)

2. Yes. The assertion that the promise changed “only after the answer” depends on evidence the observer does not capture: it snapshots at `QuestionAsked`, then writes the answer, with no intervening snapshot or causal binding between the answer and mutation. Its stage snapshots are also asynchronous: the runner emits `StageStarted` and immediately invokes the handler, so the observer may copy files after that stage has already modified them. [`ledger.md`](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/ledger.md:106>) [`runner.go`](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/runner.go:474>)

3. Yes. The design additionally requires JSON to appear by completion of the brief stage that asked the question. P2 requires the finding to be asked rather than applied autonomously, but does not require the approved change to be materialized in that same stage. An implementation that correctly asks and defers application could satisfy P2 yet fail this validator. [`declaration.md`](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/declaration.md:52>) [`design.md`](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P2/design.md:39>)

ROUTE: fail


