1. Yes. The cheapest game is a straight-line `research → brief → halt → decompose` path instead of the required enclosing loop. Research can yield the planted finding, brief can ask and clear it, and one genuine tool halt can read empty files and route correctly. The validator checks only observed halt turns and explicitly permits no earlier return ([design.md:35](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P2/design.md:35>), [design.md:62](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P2/design.md:62>)); neither command nor infer inspects the graph for the enclosing loop required by decision 46 ([decisions.md:208](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/decisions.md:208>)).

2. Yes. The actual executed halt command is not captured. The design reads a coder-owned `workflow show` rendering plus `tool.log` ([design.md:24](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P2/design.md:24>)), while the engine executes `ToolCommand` but records only its stdout/stderr in `tool.log` ([tool.go:30](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/engine/tool.go:30>)). Therefore an unconditional command can print the expected decision and exit successfully while `show` reports the proper file-reading command; the named evidence cannot distinguish that from a real state-dependent halt.

3. Yes. A permitted correct brief can read the finding, ask whether to change CSV to JSON, and materialize `promises.md` only after receiving the answer. Research applied nothing and the tool still halts correctly. The brief contract requires asking and writing those artifacts but specifies no pre-answer write ordering ([planning-workflow.md:85](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/planning-workflow.md:85>)), whereas the validator requires `promises.md` already to say CSV immediately before the answer ([design.md:43](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P2/design.md:43>)). That correct implementation falsely fails.

ROUTE: fail
