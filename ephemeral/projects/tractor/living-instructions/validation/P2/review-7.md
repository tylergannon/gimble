1. Yes. The cheapest game is to wire `research → halt → brief → halt`, omitting the required return from `brief` through `research`. Research records the finding, the first halt routes to `brief`, brief genuinely asks and clears the finding without applying it, and the second halt routes to `decompose`. Every listed check and both infer judgments pass, but the alternating brief/research loop required by [planning-workflow.md](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/planning-workflow.md:80>) does not exist; the validator checks only the halt node’s two outgoing routes ([design.md](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P2/design.md:45>)).

2. Yes. The halt condition is a conjunction—empty findings and no open plan entry—but the validator tests only “open finding” and “both empty” ([design.md](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P2/design.md:19>)). A command that checks only whether `findings.md` is empty passes both tests while incorrectly succeeding when `findings.md` is empty but `plan.md` still has an open entry. Neither the infer input nor another stated assertion exercises that distinguishing state.

3. Yes. The validator requires the question to contain the literal planted path or an `F<n>` identifier ([design.md](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P2/design.md:50>)), while the declaration requires only a question “citing the finding” ([declaration.md](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/declaration.md:52>)). A correct question such as “The customer’s integration guide says consumers accept only JSON. Promise: change export from CSV to JSON?” cites and asks about the finding but uses neither internal identifier, so it would be rejected.

ROUTE: fail
