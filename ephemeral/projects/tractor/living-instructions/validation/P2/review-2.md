1. Yes. The cheapest game is for `intake` or `brief`—not research—to create `research/findings.md` from the planted leaf, while the research branches merely run. The brief can then ask a legitimate question, retain CSV until the answer, and change it to JSON afterward. The validator checks that the finding motivated the question, but never binds creation of that finding to a research segment ([design.md](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P2/design.md:27>)).

2. Yes. The claim that research “wrote nothing outside `research/`” depends on searching raw tool arguments for two literal filenames ([design.md](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P2/design.md:39>)). The captured Codex event contains only the submitted command and cwd, not its resolved filesystem write set ([events.go](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/harness/codex/events.go:144>)). A command can construct the filename through shell variables, globbing, or a helper script and mutate it without either literal appearing in the arguments.

3. Yes. The inference requires the question to include a recommendation ([design.md](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/validation/P2/design.md:43>)), but P2 requires only a `QuestionAsked` citing the finding ([declaration.md](</Users/tyler/src/tractor/.claude/worktrees/goal-gates/ephemeral/projects/tractor/living-instructions/declaration.md:52>)). A correct implementation could ask explicitly whether to change CSV to JSON, without recommending an answer, and incorrectly fail this validator.

ROUTE: fail
