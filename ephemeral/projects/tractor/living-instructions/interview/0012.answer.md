Option 1, but for all three workflows including plan: --logs optional, default to a fresh run directory under the same state root the MCP run store already uses, and print the chosen path at start. Do not put run logs under the project directory; it is committed.