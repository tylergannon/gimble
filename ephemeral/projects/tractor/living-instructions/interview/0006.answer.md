Option 1: tractor workflow list, tractor workflow run plan --project <build> --seed <path>. Build is required and explicit, never derived. Seed is a file path. workflow run takes the same --workdir and --logs flags as run; do not invent a new logs location.