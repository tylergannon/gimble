# 0014: the design loop ceiling, and what the scope pass found

Asked by the planner (Claude, running the v2 algorithm by hand) on
2026-09-03. Batched; a recommendation each. Answer in
`0014.answer.md`, numbered.

## 1. The validation design loop has run nine laps

P5 and P7 routed pass (laps 4 and 7). The other eight fail every lap,
but the findings have narrowed: on laps 6 to 9 the reviewer answers
"no game" to question 1 or 2 for most designs and fails them on
question 3 (stricter than the promise) or on a new evidence gap it
finds in the prose. Each lap fixes the concrete ones and the next lap
finds new ones. Decision 46's rule for a loop that reaches its ceiling
is a question to the human, so:

- (a) Keep looping until all ten pass, however many laps.
- (b) After lap 10, mark a design done when the reviewer's answers to
  questions 1 and 2 are "no" and its question 3 finding is either
  fixed or recorded under Not proven. Record the ceiling in decisions.
- (c) Mark all ten done now and move on; the reviews are on file.

Recommend (b). The reviewer is doing its job; the remaining findings
are the kind a running system settles faster than prose does.

## 2. The scope pass says four things serve no promise

Pass 5 (scope) failed on: the research node's branches, index, and
quality gate (P2 exercises research but promises only one finding and
the halt); `research_auditor`, `slice_critic`, and `proof_skeptic` (P7
promises only `scope_cop`); `replan` (P6 and P10 do not need it); the
holdout writer in `design` and the handoff to `verify`. All four are
your rulings (decisions 43, 44, 46, 49). Under decision 40 and pass 5,
a slice must serve a promise, so either the brief adds promises or the
plan drops the work.

- (a) Add three promises: P11 research (two planted leaves on one
  seed; both branches' leaves, the index entry, and one finding
  exist); P12 supervisors (every supervisor in the graph emits at
  least one verdict during a plan run, recorded in the timeline); P13
  replan (after every implement lap a replan turn ran and changed only
  open sprint items). Each gets a design and a review like the others.
- (b) Record them in decisions as promise-adjacent mechanism (the
  means by which P2, P6, and P7 are met) and exempt them from pass 5.
- (c) Drop the three supervisors, the index and quality gate, and
  replan from this version.

Recommend (a). Three small scenario designs; they are cheap and they
keep the rule intact.

## 3. Is chapter 6 sprint 2's holdout "a holdout in this project's proof"?

Sprint 2 proves the holdout handoff with a small scratch package whose
one universal promise has a holdout the coder never sees. The
declaration and `promises.md` exclude "a holdout in this project's
proof". Pass 5 read sprint 2 as a violation.

- (a) It is not: the exclusion means no holdout over this project's
  own ten promises; a holdout inside a scratch package under test is
  the feature being proven. Reword the exclusion to say so.
- (b) It is: drop the holdout leg from sprint 2 and prove the handoff
  only by reading the rendered `verify` prompt.

Recommend (a).

## 4. Decision 42 and the three universal promises without holdouts

Pass 2 (consistency) found that decision 42 and planning-workflow pass
4 require a holdout for every universal promise, while the declaration
says P3, P5, and P8 are universal and carry none because their sets
are small and checked exhaustively.

- (a) Amend decision 42: a universal promise over a set the verifier
  checks exhaustively needs no holdout; the holdout is for sets too
  large to check whole.
- (b) Reclassify P3, P5, and P8 as scenarios.

Recommend (a); it is what you said ("always considered, not
universal").

## 5. Everything else pass 2, 3, 5, and 6 found

Fixed without asking: the prompt count (four), the template delimiters
and action names in the declaration, the orphan rule's scope
(doctrine pages only), chapter 4's page-list check, "content, not
code" wording, the `command`/`infer` wording in decision 47 and the
design note, the models table (supervisors and reviewers on codex
where they watch or review claude; `halt` has no model; `assemble` on
the cheap role), decision 48 marked as amended by 58, sprint backlogs
required, the build order's chapter numbers, `chapters.md` prose,
chapter completion wording, the seed deadline, `go vet` and
`golangci-lint` in the chapter commands, doctrine provenance may point
at `decisions.md`, chapter 5 backlog entry 6 split in two, sprint 3's
pages written by Claude before the run and staged under
`chapters/04-library/content/`, "docs and skill" sprints made
exercisable (an agent follows the docs to do one thing), the proof
record folded into the seed sprint, and the proof script's Go call
fixed. Say so if any of those should have been a question.
