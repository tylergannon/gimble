Option 1, recommendation.md with that fixed contract. Fix the names now, not placeholders: the three embedded workflows are plan, medium, and large, so Next is 'tractor workflow run medium --project <build>' or the large equivalent. Chapter 3 builds to those names.