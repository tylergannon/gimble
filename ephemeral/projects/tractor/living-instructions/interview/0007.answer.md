Option 1. The same node writes the artifacts. After it, one tool node checks mechanically that brief.md, checklist.md, and the recommendation exist and that checklist.md parses as a loop checklist; on failure it routes back to the interview node, on success to success. No second agent node.