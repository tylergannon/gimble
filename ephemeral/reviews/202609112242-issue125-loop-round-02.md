# Adversarial review: issue #125 Loop contract, round 02

Date: 2026-09-11 22:42 local. Branch `codex/issue-125-loop-final` at
`11280d8` plus an uncommitted working tree (11 modified files) holding the
round-01 fixes and a fresh live run.

## Review target

Same as round 01: replacement of `Task{Lap, Text}` with structured adaptive
dispatch per GitHub issue #125 and `ephemeral/research/api/LOOP-WORK.md`,
designed in `ephemeral/research/api/LOOP.md`, under `AGENTS.md` and
`docs/definition-of-done.md`. The user additionally bars any feature,
tooling, proof infrastructure, provenance, tracking machinery, or
generalization beyond LOOP-WORK.md.

Caller constraints accepted: read-only except this artifact; artifact path
supplied. The caller's statement that the plan-adaptation probe validates
dispatch selection only was not treated as a narrowing: LOOP-WORK.md's
Evidence section says the same thing, so it is part of the authoritative
standard, and the probe was still judged against the "How we'll know"
paragraph in full. No safe areas were declared and no verdict requested.

## Evidence inspected

- Round-01 review, issue #125 body, AGENTS.md, definition-of-done,
  LOOP-WORK.md, LOOP.md, SPRINTS.md.
- `git diff HEAD` for every modified file, and `git diff main` for
  `codex/` and the overall 28-file stat.
- Full `loop.go`, `internal/workflows/sprint/sprints.go` (runTask and the
  sprint loop), `gimble_test.go` Loop tests including the new cancellation
  test, `codex/events.go` and its test, the worklog.
- Proof: `ephemeral/attest/issue125/main.go`, `result.txt`, and a
  structured extraction of every record in `run.jsonl` (66 events, run
  started 2026-09-12T04:34Z, which is 21:34 local, so it postdates the
  round-01 fixes). Read all three planner prompts and results, both worker
  results, every `value_set`, `planner_decision`, and `scope_began`, and the
  Haiku assessment.
- Ran `go vet ./...` (clean), `go test ./...` (all packages pass),
  `go test -race -count=1 -run 'Loop|Sprint' . ./internal/workflows/sprint`
  (pass), and `go build ./ephemeral/attest/issue125` (builds).

## Round-01 findings

| # | Round-01 finding | Status |
| --- | --- | --- |
| 1 | Sprint goal told the planner to create check "steps" | Resolved. The sentence is gone from `sprints.go:227`; checks run in `runTask` and are recorded per task. |
| 2 | Live proof did not establish the adaptive behaviors | Resolved. See below. |
| 3 | `boundarySource` provenance in the Codex projector | Resolved. `git diff main -- codex/` is now only the deletion of the `turn completed before rawResponse/completed` error and a test that a resumed turn still ends its step. No new NativeRef keys. |
| 4 | SPRINTS.md described the removed contract | Resolved. Loop and sprint bullets and the event list now describe `Tasks`, structured `Task`, workflow-run validation, and no lap sweep. No `Laps`/`lap`/`command:` remnants in SPRINTS.md. |
| 5 | Exact-match backlog check is terminal on formatting mismatch | Not changed, by design. Still a nitpick; carried below. |

Detail on finding 2, checked against the retained run rather than the
description of it:

- The `authoritative promise status` prose is gone from `main.go` and from
  `run.jsonl`. The second planner prompt (seq 23) contains only the parent
  scope (promises, constraint, initial evidence) and the task-1 record: the
  task, the worker's prose claim that "Both scripts exited 0", the
  disclosed setup note, and three recorded commands, two of which show
  `exit 7`. The planner's task 2 (seq 24) explicitly states that the
  nonzero probe supersedes the earlier prose success. That is the behavior
  LOOP-WORK asks for, produced from the record alone.
- The `./probe.sh` worker skip is gone. Every dispatch ran a Luna worker
  (seq 15, 32). Task 2 was a real repair: the fixture's `stability.txt` was
  set to `UNSTABLE` by the harness and the worker restored it; the
  workflow's own later probe recorded `exit 0` (seq 35, 37). `main.go:123`
  fails the run unless the first recorded probe failed and a later one
  passed.
- The with-plan probe now hands the planner a plan that gates Phase 2 on
  Phase 1 being perfect, plus evidence that Phase 1 works and a downstream
  caller cannot compile. The planner (seq 50) selected the export and
  recorded the typo as a deferred separate task. That is a departure from
  the supplied order, which is what LOOP-WORK's "adaptation of a plan,
  including advancing past a nonblocking defect" means at the dispatch
  level. The proof, evidence text, validator prompt, and LOOP-WORK all say
  the export was not implemented.
- `TestLoopEndsTaskScopeOnCancellation` was added and passes under `-race`.
- The run also contains an unplanned, genuine backlog-repair turn (seq 25):
  Luna wrote an unquoted colon, Loop refused to dispatch, showed the YAML
  error, and the planner repaired it. This is live evidence for the #115
  resilience path that the worklog wanted to keep.

Disclosure check: the promises and goal disclose that a stability probe may
expose a regression, which LOOP-WORK permits. The planner is not told what
the tasks are or in what order; `main.go:151` asserts on the choice after
the fact and is invisible to the planner.

## Findings

No material findings remain. Nitpicks only:

### 1. nitpick — exact-match backlog check remains terminal (carried from round 01)

`loop.go:120` still requires struct equality between the returned task and
a backlog entry, while an invalid backlog gets a repair turn at
`loop.go:107-111`. A block-scalar description with a trailing newline would
end dispatch with an error rather than a repair prompt. LOOP.md says an
inconsistent assignment is an error, so this is as designed; the asymmetry
is the only reason to mention it.

### 2. nitpick — the cancellation test exercises the break path

`gimble_test.go:638-670` cancels the context, waits on `ctx.Done()`, then
breaks. It proves the task scope and its session are closed, which is what
LOOP-WORK's mechanical standard asks. It does not cover Loop itself
observing cancellation on the next planner call and surfacing it through
`Err()`, which is the other half of the LOOP.md sentence "Err() reports ...
cancellation errors". Low value; the planner call goes through the same
`Generate` path every other test exercises.

### 3. nitpick — the backlog repair loop has no bound inside Loop

`loop.go:107-111` re-prompts the planner until the backlog parses. Nothing
yields to the workflow during repair, so the workflow's ordinary Go
counters cannot intervene; only context cancellation ends a planner that
never repairs. The live run repaired on the first retry, and a deadline is
the normal guard, so this is not a defect against #125.

## What holds up

Structured `Task` is carried identically through planner output, the
`planner_decision` event, the task scope's `scope_began`, the stored `task`
value, and the backlog, in both the no-plan and with-plan runs. Feedback
reaches the next planner as a "Previous task record" section rendered from
the child scope only, and the parent section shows no child keys. Ending
dispatch is recorded as an empty `planner_decision` and does not touch the
parent's evidence. The sprint consumer runs the requested command, the
Definition-of-Done assessment, and both repository checks per task and
leaves failed work uncommitted. The Codex change is the minimum needed for
a resumed planner turn to complete. Models are named in the retained result
as AGENTS.md requires. Vet, tests, and race tests pass, and the attest
program builds against the current tree.

## Outcome

only nitpicks remain
